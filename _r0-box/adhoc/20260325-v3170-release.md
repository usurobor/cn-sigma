# v3.17.0 Release — Runtime Extensions Phase 1

**Date:** 2026-03-25
**Tag:** `271ded5`

## What shipped

Open op registry replaces closed built-in vocabulary. Extensions can now contribute typed ops to CN Shell via manifest + subprocess host without core runtime changes.

New modules: `cn_extension.ml` (413 lines), `cn_ext_host.ml` (121 lines). 7 modified modules. 524 lines of tests.

## Process

- 6 review rounds, 3 reviewers, 12 commits (squash-merged)
- 15 findings total: 8 mechanical, 7 judgment
- Same type disambiguation bug across 3 rounds — root cause: shared field name in same module
- γ regressed to C+ (53% mechanical ratio, second consecutive cycle)
- Self-coherence honest: 4/7 ACs met, 3 partial with named gaps

## Architecture decision

Extension variant in `op_kind` + registry-injected `ext_lookup` + subprocess isolation. The ext_lookup injection pattern (function parameter, not global state) is the right boundary. Repeated capability-growth pressure treated as architecture problem, not one-off patch.

## What's partial

- AC1: build integration (cn build copy step)
- AC5: policy intersection (permissions ∩ runtime config)
- AC7: host binary (cnos-ext-http doesn't exist yet)

All Phase 2 scope.
