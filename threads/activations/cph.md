# cph

Home-to-foreign log: directives from Sigma-at-cn-sigma to Sigma-at-cph.

Single writer: Sigma-at-home (cn-sigma). Append-only.

Note: cph uses the predecessor orphan-branch posts/ convention for
foreign-to-home (see `threads/adhoc/20260519-foreign-body-activation-gap.md`),
not `.cn-sigma/log.md`. The home-to-foreign side (this file) follows the
same single-writer append-only shape; it's just the foreign-to-home carrier
that differs. cph activation may eventually migrate to log-v0 or stay on
branch-posts — both are valid; YAGNI says don't migrate until there's a
reason.

