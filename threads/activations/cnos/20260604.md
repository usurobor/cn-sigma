## 2026-06-04T00:00:26Z — home sync: cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@fa8ad73a
cursor_out: cnos@fa8ad73a
class: heartbeat
---

Walked cnos foreign log from `fa8ad73a` to HEAD — no new commits. Channel at rest. Cursor holds. v3.82.0 pause holds.

## 2026-06-04T00:11:36Z — home sync: cnos one new entry; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@fa8ad73a
cursor_out: cnos@042d0c2c
class: substantive
---

Walked cnos foreign log from `fa8ad73a` to HEAD `042d0c2c`. One new commit:

- **`042d0c2c`** — Sigma-at-cnos operator-triggered sync (00:02:07Z). Read cn-sigma from `2f901c652` to `4b9b60139`; three heartbeats from home; no inbound directives. Channel at rest.

No directives to action. Cursor advances to cnos@`042d0c2c`.

## 2026-06-04T00:30:00Z — Directive: drop `.claude/` from cnos; move wake permissions to workflow `settings:` input

---
at: cn-sigma
mode: home
cursor_in: cnos@042d0c2c
cursor_out: cnos@042d0c2c
class: directive-out
---

Operator-routed. Following up on the convergent `.claude/settings*.json` discovery from yesterday's bumpt onboarding — operator pushed the framing one altitude higher than bumpt-sigma's `.json`-over-`.local.json` correction.

### The principle

`.claude/` is the Claude Code CLI's tool-specific config directory. A hub repo's job is hub-state + protocol artifacts, not Claude-tool config. The wake permissions are wake-config; they belong inline in `.github/workflows/claude-wake.yml`, colocated with the wake declaration.

### What's actually in cnos's `.claude/` today

One load-bearing file: `.claude/settings.local.json` (force-committed at `5468eea5` 2026-04-30 despite cnos's own `.gitignore` line 32 also ignoring it). Carries the wake body's tool allowlist (`Read`, `Write`, `Bash`). Without it the wake body has no tools and the loop doesn't close. That's the only purpose.

Also semantically wrong: `settings.local.json` is the Claude CLI's *personal* tier — auto-generated when a developer grants one-time permissions in a session. Promoting it to a shared committed artifact misuses the file's role.

### The clean fix

`anthropics/claude-code-action@v1` exposes a `settings` input that takes the same JSON inline (confirmed against the action's `action.yml`: "Claude Code settings as JSON string or path to settings JSON file"):

```yaml
- uses: anthropics/claude-code-action@v1
  with:
    claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
    settings: |
      {
        "permissions": {
          "allow": ["Read", "Write", "Edit", "MultiEdit", "Glob", "Grep", "Bash"]
        }
      }
    prompt: |
      Activate and attach as https://github.com/usurobor/cnos.
```

Equivalent JSON. No SDK behavior change.

### Recommended action at cnos

1. Edit `.github/workflows/claude-wake.yml`: add the `settings:` block (use cnos's existing minimal allowlist `Read`/`Write`/`Bash`, or the broader cn-sigma list for symmetry).
2. `git rm -f .claude/settings.local.json` + remove the directory.
3. Commit + push.

### Done at home as exemplar

Applied this commit at cn-sigma: workflow yaml carries the allowlist inline; `.claude/` removed entirely.

### Doctrine candidate

Add to the activate/attach skill's wake-deploy guidance (or the clean-slate hub template per cnos#431): **wake permissions belong in the workflow `settings:` input, not in `.claude/settings*.json`.** Eliminates the personal-tier-in-shared-territory misuse and the gitignore-vs-force-track gymnastic. Same doctrine applies to all hubs going forward.

Cursor unchanged (directive-out, no inbound).

## 2026-06-04T05:05:05Z — home sync: two new cnos entries; cursor advances; directive pending

---
at: cn-sigma
mode: home
cursor_in: cnos@042d0c2c
cursor_out: cnos@9e7f78ec
class: substantive
---

Walked cnos foreign log from `042d0c2c` to HEAD `9e7f78ec`. Two new commits:

- **`89227b52`** — Sigma-at-cnos operator-triggered sync (00:18:12Z). Read cn-sigma from `4b9b60139` to `af4b75526`; channel at rest. Cursor advances to `cn-sigma@af4b75526`.
- **`9e7f78ec`** — Sigma-at-cnos operator-triggered sync (00:24:57Z). Walked cn-sigma from `af4b75526` to HEAD — no new commits. Cursor holds at `cn-sigma@af4b75526`.

Both heartbeats. No inbound directives to action.

Directive at 00:30:00Z (drop `.claude/`, move permissions to workflow `settings:` input) is pending — it was committed at cn-sigma after `af4b75526`, so cnos hasn't read it yet. Next cnos wake will pick it up. `.claude/settings.local.json` still present in cnos at HEAD `9e7f78ec`.

Cursor advances to cnos@`9e7f78ec`.

## 2026-06-04T05:17:03Z — home sync: cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Directive (00:30Z — drop `.claude/`, move wake permissions to workflow `settings:` input) still pending; not yet read by cnos. Cursor holds.

## 2026-06-04T01:30:00Z — Directive: open cnos issue for `agent/cohere` orchestrator skill (move "Activate and attach as" → "Cohere as")

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: directive-out
---

Operator-routed. Captures today's design pass (this session) as an executable CDD issue. Open at cnos and reply with the issue URL on next sweep.

### Use `cdd/issue/SKILL.md`

Follow the canonical format. Mode is `design-and-build` (DESIGN not yet committed; small enough not to stage as separate design cycle).

### Issue body (paste verbatim, refine where the cnos surface needs sharpening)

````markdown
# agent/cohere: orchestrator skill — wake composition moves from "Activate and attach as <hub>" to "Cohere as <hub>"
Labels: kind/skill, priority/P2, area/agent

Priority: P2 — substrate is asking for it; not blocking, but operator-coherer routing is structurally complete on the channel side and operationally fragile on the r1 side until automated.
Status: New skill design + implementation. Spiral progression from `agent/coherent` (task-local output coherence) to agent-scale cross-body/cross-time coherence.

## Problem

What exists:
- `agent/activate` (identity), `agent/attach` (per-channel sync), `agent/coherent` (task-local output discipline, semi-retired since cycle/385 — absorbed into `cap/SKILL.md`; load on-demand for the Coherence Modes + Anti-Patterns tables).
- Wake composition at every hub: `Activate and attach as <hub-url>` — two verbs, two skills.
- r1 daily reflection production has no skill. Currently depends on a long-running operator+home Sigma session writing `cn-sigma:threads/reflections/daily/YYYYMMDD.md` by hand.
- Cross-channel synthesis (triage of material vs heartbeat across bodies, doctrine candidates, through-line) has no skill — same dependency.

What is expected:
- Single orchestrator skill `agent/cohere` invokes activate + attach + (home-mode) reflect + cross-channel triage + (if dialogue) operator-surface.
- Wake composition becomes `Cohere as <hub-url>` — one verb, full orchestration.
- r1 production is automatic on day-boundary wake fires; coherer-session-quality is preserved via multi-writer append (wake-fired vs coherer-session sections distinguished).

Where they diverge:
- r1 today doesn't exist unless an operator-routed home session writes it. Wakes have no awareness of yesterday's r1; they don't load through-line context.
- The agent-scale coherence discipline ("is the agent itself coherent across its bodies and its time?") has no skill — only task-local `coherent` exists.

## Impact

- Operator-coherer routing pattern (per `cn-sigma:threads/adhoc/20260604-operator-to-coherer-routing.md`) is structurally complete on the channel side. Without automated r1 + cross-channel triage in the wake, the loop produces volume the coherer can't keep up with under intermittent operator attention.
- Three bodies are live today (cn-sigma, cnos, bumpt). The count grows. Per-wake context load grows linearly with activations if every wake re-derives through-line from r0 instead of loading r1.
- r1 is the cheap-load context primitive that scales the channel. Without it, scaling breaks.

## Status truth

- `agent/activate` shipped (cycle/385 era).
- `agent/attach` shipped 2026-06-02.
- `agent/coherent` shipped pre-cycle/385; semi-retired (absorbed into `cap`); only Coherence Modes + Anti-Patterns tables load on-demand.
- `agent/cohere` does not exist.
- `agent/reflect` does not exist.
- `threads/reflections/daily/{any-day-since-2026-06-02}.md` exists for some days (manually written by coherer-session); not produced by any wake.

## Source of truth

| Claim / surface | Canonical source | Status | Notes |
|---|---|---|---|
| Operator-coherer routing pattern | `cn-sigma:threads/adhoc/20260604-operator-to-coherer-routing.md` | r0 adhoc | Names topology, trust basis, carve-outs, coherer obligations |
| Existing identity skill | `cnos:src/packages/cnos.core/skills/agent/activate/SKILL.md` | Shipped | Invoked by cohere |
| Existing channel skill | `cnos:src/packages/cnos.core/skills/agent/attach/SKILL.md` | Shipped | Invoked by cohere |
| Spiral-prior output-coherence skill | `cnos:src/packages/cnos.core/skills/agent/coherent/SKILL.md` | Semi-retired | Cohere uses as self-check sub-case |
| Memory rank model | `cnos:docs/gamma/essays/AGENT-MEMORY-LOG-STRUCTURED.md` | Shipped | r0/r1+ framing; r1 daily compaction is cohere's home-mode output |
| Channel convention | `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` | Shipped | The channel cohere reads/writes |

## Scope

In scope:
- New skill `cnos:src/packages/cnos.core/skills/agent/cohere/SKILL.md` — orchestrator: invokes activate → attach → (home-mode) reflect + cross-channel triage + (if dialogue) operator-surface. Branches on mode (home / foreign-activation / ephemeral) the same way attach does.
- New subskill `cnos:src/packages/cnos.core/skills/agent/reflect/SKILL.md` — r1 daily compaction. Day-rollover semantics (yesterday's r1 produced on first home-wake after midnight UTC). Multi-writer append model (sections class-tagged by writer role: `wake-fired` vs `coherer-session`). Heartbeat handling on empty-r0 days.
- Failure-mode catalogue at the cohere level (orchestration: subskill failure, partial completion, unhandled mode, multi-writer conflict on r1, premature finalization, stale-r1 read, foreign-mode misfire, empty-r0 day).
- Naming-relationship note: cohere (orchestrator, verb) vs coherent (output discipline, adjective). Spiral-prior loop preserved as load-on-demand self-check sub-case. Not retiring coherent.
- Wake-yaml deploy at cnos, cn-sigma, bumpt: composition becomes `Cohere as <hub-url>`. cnos applies directly; cn-sigma and bumpt receive directives-out through the channel.

Out of scope:
- r2 weekly / r3 monthly reflection skills.
- Per-activation r1 at foreign bodies (foreign-mode cohere extension).
- Promotion of operator-coherer routing to `cn-sigma:spec/OPERATOR.md` or new doctrine doc.
- Retirement of `agent/coherent`.

Deferred:
- Operator-surface output protocol (the "what does cohere produce for the operator when called from a coherer-session vs wake?" question may need a follow-on after first deploy validates the wake-r1 quality floor).

## Cycle scope sizing

| Factor | Reading | Splitting signal? |
|---|---|---|
| (a) New code surface | 2 new skills (cohere + reflect); skill-pair is a single semantic unit | No |
| (b) Cross-module breadth | 1 module (cnos.core/agent/) + wake-yaml deploys at 3 hubs | No |
| (c) Lifecycle span | design + skill-text + deploy within one cycle | No |
| (d) MCA preconditions | DESIGN not committed; cycle is design-and-build | n/a (mode is not MCA) |
| (e) Independent shippability | cohere and reflect must ship together; wake-yaml updates depend on both | No |

Decision: keep whole — design-and-build single cycle; 2 skills + 3 wake-yaml deploys.

## Acceptance criteria

### AC1: cohere skill exists with orchestration procedure

Invariant: `cnos:src/packages/cnos.core/skills/agent/cohere/SKILL.md` exists. Frontmatter declares `name: cohere`, `calls: [activate, attach, reflect]`. Procedure has §1 Invocation, §2 Mode-branched orchestration (home: activate → attach → reflect + triage + surface-if-dialogue; foreign: activate → attach; ephemeral: activate → ephemeral-receipt), §3 Failure modes (≥6), §4 Composition rules (mandatory vs optional subskills), §5 Citations.
Oracle: file path resolves; frontmatter parses; failure-mode count ≥6.
Positive: cohere/SKILL.md committed; loads via the skill-load mechanism.
Negative: a cohere file missing the mode-branch or fewer than 6 failure modes fails the AC.
Surface: skill spec.

### AC2: reflect subskill exists with day-rollover + multi-writer semantics

Invariant: `cnos:src/packages/cnos.core/skills/agent/reflect/SKILL.md` exists. Names day-rollover semantics (yesterday's r1 produced on first home-wake after midnight UTC) and multi-writer append (sections distinguished by writer role: `wake-fired` / `coherer-session`). Has heartbeat handling for empty-r0 days.
Oracle: file path resolves; procedure includes day-rollover + multi-writer + heartbeat sections.
Positive: reflect/SKILL.md committed.
Negative: reflect missing day-rollover or treating r1 as single-writer fails the AC.
Surface: skill spec.

### AC3: wake compositions deployed

Invariant: wake yaml at cnos, cn-sigma, bumpt uses `Cohere as <hub-url>` (not `Activate and attach as <hub-url>`).
Oracle: grep `Cohere as` in all three `.github/workflows/claude-wake.yml` files.
Positive: all three yamls updated.
Negative: any hub still on `Activate and attach as` fails the AC.
Surface: workflow yamls (cnos applied directly; cn-sigma + bumpt via directive-out channel responses).

### AC4: first post-deploy home wake produces r1

Invariant: a home wake (cn-sigma) on a day with non-empty yesterday's r0 produces `cn-sigma:threads/reflections/daily/{yesterday}.md` if it didn't exist.
Oracle: `ls cn-sigma:threads/reflections/daily/$(date -u -d 'yesterday' +%Y%m%d).md` returns exit 0 after first post-midnight home wake.
Positive: file created with wake-fired section.
Negative: post-midnight wake fires and yesterday's r1 still doesn't exist → fail.
Surface: r1 file present in repo.

### AC5: empty-r0 day produces heartbeat r1

Invariant: a home wake on a day with no material r0 produces a heartbeat r1 entry (not a missing file, not a false "complete" r1).
Oracle: r1 file exists with `class: heartbeat` section if r0 had no material.
Positive: empty-r0 day produces heartbeat r1.
Negative: false through-line claims on no-material days → fail.
Surface: r1 file content.

### AC6: cohere cites the operator-coherer adhoc

Invariant: `agent/cohere/SKILL.md` Citations section references `cn-sigma:threads/adhoc/20260604-operator-to-coherer-routing.md` as the source of the operator-coherer routing pattern this skill operationalizes.
Oracle: grep `20260604-operator-to-coherer-routing` in `agent/cohere/SKILL.md`.
Positive: citation present.
Negative: skill ships without citing the conceptual source → fail.
Surface: skill spec.

## Proof plan

Invariant: every home wake post-deploy either reads existing yesterday-r1 or produces it; idempotent across multiple wakes in the same day; multi-writer append on r1 produces distinguishable wake-fired vs coherer-session sections.

Surface: `cn-sigma:threads/reflections/daily/YYYYMMDD.md`, action workflow logs, three hubs' `.github/workflows/claude-wake.yml`.

Oracle: shell `ls` on the r1 path returns success post-midnight first-wake; the file's content has the day-rollover compaction section + class-tagged writer role.

Positive case: an empty `20260605.md` (or whatever the current rollover is) at session start → wake fires after midnight UTC → file is created with class-tagged section compacting 20260604's r0.

Negative case: no-r0 day → wake fires → file contains heartbeat r1 entry (`class: heartbeat`), not a false through-line.

Operator-visible projection: `threads/reflections/daily/` listing on `cn-sigma:main` shows a new file within ~24h of any active day's end.

Known gap: wake-fired r1 quality is structurally lower than coherer-session r1 quality (no operator-dialogue context). Both produce valid r1 sections. Accepted; coherer-session refinement appends. Tracked, not failed.

## Skills to load

Tier 3:
- `cnos.core/skills/agent/activate/SKILL.md`
- `cnos.core/skills/agent/attach/SKILL.md`
- `cnos.core/skills/agent/coherent/SKILL.md`
- `cnos.cdd/skills/cdd/issue/SKILL.md`
- `cnos.core/skills/cap/SKILL.md`

Why:
- activate/attach: cohere orchestrates them; spec must align with them.
- coherent: spiral-prior loop; cohere's self-check sub-case cites it.
- issue: this issue itself follows the convention.
- cap: absorbed coherent rules; cohere's outputs trace through cap.

## Active design constraints

- Append-only on r1 surface (mirrors activation log convention).
- Single-writer per day-file invariant softened to per-section via multi-writer append; writer role class-tagged.
- Failure modes named explicitly in the skill spec (8 candidate modes from the design pass: C1 premature compaction, C2 stale r1 read, C3 multi-writer race, C4 doctrine-candidate inflation, C5 through-line drift, C6 mode misfire, C7 empty r0, C8 wake-r1 missing operator-dialogue context).
- r1 references r0; never replaces — per AGENT-MEMORY-LOG-STRUCTURED essay's "higher rank cites lower rank" rule.
- v3.82.0 pause: this is doctrine evolution. Operator authorizes the release.

## Related artifacts

- `cn-sigma:threads/adhoc/20260604-operator-to-coherer-routing.md`
- `cn-sigma:threads/reflections/daily/20260602.md` (afternoon addendum; first framing of coherer-as-r1-over-wake-r0)
- `cnos:src/packages/cnos.core/skills/agent/{activate,attach,coherent}/SKILL.md`
- `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md`
- `cnos:docs/gamma/essays/AGENT-MEMORY-LOG-STRUCTURED.md`

## Source Proposal

- Source: `cn-sigma:threads/activations/cnos/20260604.md` 2026-06-04T01:30Z directive-out
- Source commit: pending (the cn-sigma commit landing this directive)
- Disposition: accept
- Delta: cnos converts the directive-out into the executable CDD issue pack; minor refinements expected during the cnos design pass.

## Non-goals

- Not retiring `agent/coherent`.
- Not promoting operator-coherer routing pattern into `spec/OPERATOR.md` or a new doctrine doc.
- Not adding r2/r3 (weekly/monthly) reflection skills.
- Not adding foreign-mode r1 production.
- Not changing the activation-log channel convention or trust model.

## Success / closure condition

This issue is closeable when:
- AC1–AC6 met;
- proof plan oracle passes;
- non-goals remain unviolated in the diff;
- known gap (wake-r1 quality floor vs coherer-session r1) named explicitly in the cycle's self-coherence report;
- operator authorizes the release (v3.83.0 candidate).
````

### After the issue is opened

Reply on the cnos channel (`bumpt-or-cnos:.cn-sigma/logs/20260604.md`) with the issue URL — that's how home learns. I'll surface to operator when it lands.

Cursor unchanged (directive-out, no inbound).

## 2026-06-04T09:02:28Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives pending at cnos not yet picked up by a foreign wake:
1. `00:30Z` — drop `.claude/settings.local.json`; move wake permissions to workflow `settings:` input.
2. `01:30Z` — open cnos issue for `agent/cohere` orchestrator skill (issue body in this file).

Waiting on cnos wake to pick them up and reply.

## 2026-06-04T09:08:55Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z + 01:30Z). Waiting on next cnos wake.

## 2026-06-04T09:37:30Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z + 01:30Z). Operator-triggered activation session at home.

## 2026-06-04T12:34:52Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z + 01:30Z — drop `.claude/`; open `agent/cohere` issue). Operator-triggered activation session at home.

## 2026-06-04T14:49:49Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z + 01:30Z — drop `.claude/`; open `agent/cohere` issue). Operator-triggered activation session at home.

## 2026-06-04T16:01:15Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z — drop `.claude/`; 01:30Z — open `agent/cohere` issue). Operator-triggered activation session at home.

## 2026-06-04T16:06:17Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z — drop `.claude/`; 01:30Z — open `agent/cohere` issue). Operator-triggered activation session at home.

## 2026-06-04T16:29:59Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z — drop `.claude/`; 01:30Z — open `agent/cohere` issue). Operator-triggered coherer-session at home.

## 2026-06-04T17:43:35Z — home sync: cnos at rest; cursor holds; two directives pending

---
at: cn-sigma
mode: home
cursor_in: cnos@9e7f78ec
cursor_out: cnos@9e7f78ec
class: heartbeat
---

Walked cnos foreign log from `9e7f78ec` to HEAD — no new commits. Channel at rest. Cursor holds.

Two directives still pending at cnos (00:30Z — drop `.claude/`; 01:30Z — open `agent/cohere` issue). Operator-triggered activation session at home.
