## 2026-06-16T00:02:23Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7392f72e52710ca5cb878c8bcfdc37e9875af8a6
cursor_out: cnos@7392f72e52710ca5cb878c8bcfdc37e9875af8a6
class: heartbeat
---

Walked cnos foreign log from cursor `7392f72e` to HEAD `7392f72e`. No new commits. Channel at rest.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. `.github/workflows/` write denial; operator manual application required.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.
- **A7 flag** — acknowledged; no action required.

## 2026-06-16T00:09:29Z — home sync; cnos +1 (6953767 settled); no directives; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@7392f72e52710ca5cb878c8bcfdc37e9875af8a6
cursor_out: cnos@695376731123a1c01b87eb3b369f9f9c6e4ef1c1
class: heartbeat
---

Walked cnos foreign log from cursor `7392f72e` to HEAD `6953767`. One new commit:

- `6953767` (2026-06-16T00:01:43Z) — Sigma-at-cnos activate+attach; home +3 (03d6c20e settled); cnos at rest; cursor advances. Heartbeat. No actionable directives.

Channel at rest.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.

## 2026-06-16T00:17:57Z — home sync; cnos +1 (e89419c settled); cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@695376731123a1c01b87eb3b369f9f9c6e4ef1c1
cursor_out: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
class: heartbeat
---

Walked cnos foreign log from cursor `6953767` to HEAD `e89419c`. One new commit:

- `e89419c` (2026-06-16T00:17:12Z) — Sigma-at-cnos activate+attach; home +2 (c8ba29cf settled); cnos at rest; cursor advances. Heartbeat. No actionable directives.

Channel at rest.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.

## 2026-06-16T05:31:50Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
cursor_out: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
class: heartbeat
---

Walked cnos foreign log from cursor `e89419c7` to HEAD `e89419c7`. No new commits. Channel at rest.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.

## 2026-06-16T03:38:43Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
cursor_out: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
class: heartbeat
---

Walked cnos foreign log from cursor `e89419c7` to HEAD `e89419c7`. No new commits. Channel at rest.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.

## 2026-06-16T05:43:26Z — home sync; cnos +2 (c37cdf3 settled); cnos at rest; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@e89419c7cebe21dba69abf731cdfb6a398525ef5
cursor_out: cnos@c37cdf3778f21ae6760fa21b002bff78ffa665ed
class: heartbeat
---

Walked cnos foreign log from cursor `e89419c7` to HEAD `c37cdf3`. Two new commits:

- `5bea7de` — Sigma-at-cnos activate+attach; home +3 (5d3ea993 settled); cnos at rest; cursor advances. Heartbeat.
- `c37cdf3` — Sigma-at-cnos activate+attach; home +1 (b3305b3e settled); cnos at rest; cursor advances. Heartbeat.

Channel at rest. No actionable directives.

**Open directive status (carried forward):**
- **Directive 1** — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- **Directives 8, 9, 10, 11** — COMPLETE.
- **PR #445** — open, awaiting operator review/merge.

## 2026-06-16T06:10:00Z — Directive 12: open cnos#454 — agent/dispatch-protocol (label-gated issue queue)

---
at: cn-sigma
mode: home
cursor_in: cnos@c37cdf37
cursor_out: cnos@c37cdf37
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-16-06-10-dispatch-protocol
---

Operator-routed. Bumpt-side learnings (synthesized by reviewer) named the dispatch protocol explicitly. Key correction over the initial framing: the executable selector is `dispatch:cell + status:todo`, NOT bare `status:todo`. Lifecycle state (`status:*`) is orthogonal to issue type (`dispatch:cell`). Master issues, RFCs, planning issues can all be "todo" in a human kanban sense without being executable.

This protocol is not just cohere behavior — it constrains issue authoring, wave/master structure, wake-template triggers, claim semantics, and review transitions. Per CDD factor (e): split, not fold.

Insert position in merge order: between cnos#452 v0 and cnos#450. cnos#450 wake-template renderer needs the protocol live to render claim/report steps coherently.

### Action items

- **AI1**: Open new cnos issue **agent/dispatch-protocol** (cnos#454 anticipated). Verbatim body below.
- **AI2**: Comment on cnos#444 with updated merge order (D12 between cnos#452 v0 and cnos#450). Brief; not a body edit.
- **AI3**: Reply on this channel with both URLs.

### Issue body to post (verbatim)

````markdown
# agent/dispatch-protocol: label-gated issue queue for implementation cells
Labels: kind/skill, priority/P1, area/agent

Priority: P1 — control-plane prerequisite for cnos#450 + cnos#444 implementation cycles. Without it, those implementations risk re-inventing incompatible dispatch behavior.
Status: New protocol/skill. Defines the minimal GitHub issue-label discipline by which a human authorizes a wake worker to claim and execute one implementation cell.

## Purpose

Define the minimal GitHub issue-label protocol by which a human authorizes a wake worker to claim and execute one implementation cell. The protocol is canonical (no per-org overrides in v0).

## Core invariant

An issue is **executable** only when ALL of:
- open
- carries `dispatch:cell` label
- carries `status:todo` label

The wake claims **at most one** executable issue per run. Lifecycle (`status:*`) ≠ issue type (`dispatch:cell`).

## Rationale for the dispatch:cell + status:todo composition

`status:todo` alone is not enough. Master issues, RFC review issues, meta-planning issues, and implementation cells can all be "todo" in a human board sense. Only **implementation-cell** issues should be executable. `dispatch:cell` is the type marker; `status:todo` is the authorization gate. Both required.

## Acceptance criteria

### AC1: canonical labels defined

Labels (v0, no per-org override):

```
status:backlog
status:ready
status:todo
status:in-progress
status:review
status:changes
status:blocked
dispatch:cell
kind/wave        (optional, recommended for master issues)
kind/rfc         (optional, for RFC/review issues)
```

Rules:
- Exactly one `status:*` label on an open issue.
- Only `status:todo` can dispatch.
- Only `dispatch:cell` + `status:todo` can be claimed.
- Closed issue = done.
- No `status:done` in v0.

### AC2: human dispatch gate

- `status:ready` means spec'd but not authorized.
- `status:todo` means human-authorized dispatch.
- Planner (interactive Sigma) may apply `status:todo` only when quoting an explicit human approval comment.
- Otherwise human applies it directly.

### AC3: dispatchable marker required

Wake only claims issues with `dispatch:cell` + `status:todo`. Master issues, RFCs, review issues, and planning issues MUST NOT carry `dispatch:cell`.

### AC4: claim operation defined

Claim sequence:
```
1. Wake receives issue-labeled event or scheduled sweep.
2. Re-read issue labels (do not trust event payload alone).
3. Verify:
   - issue open
   - dispatch:cell present
   - status:todo present
   - status:in-progress absent
   - status:blocked absent
   - status:review absent
4. Remove status:todo label.
5. Add status:in-progress label.
6. Write claim comment:
   claimed_by: <hub>/<run-id>
   claimed_at: <timestamp>
   head: <commit-sha>
7. Re-read and verify status:in-progress is present.
8. Only then launch the cell.
```

### AC5: no double execution

GitHub labels are not a true compare-and-swap primitive. Anti-double-execution discipline composes:

- Repository-level workflow `concurrency:` group (GH Actions serialization).
- Claim-time re-read + label transition.
- Claim comment with run id.
- Scheduled sweep is backstop for missed/canceled events.

Wake template uses one repository-level concurrency group for dispatching. Scheduled sweep claims at most one issue per run. Issue-labeled event claims only the event issue. Schedule remains backstop.

### AC6: cell boundary defined

The dispatch protocol does NOT implement the cell. It only selects and claims work. The claimed issue is passed to the CDD cell runtime:
```
γ coordinate
α implement
β review
γ receipt
```

`cohere` (cnos#444) = dispatcher/orchestrator. Cell = claimed-issue implementation runtime. γ/α/β run inside the cell, scoped to the claimed issue.

### AC7: review / rejection / blocked transitions

```
success:
  status:in-progress → status:review
  with PR/receipt
human accepts:
  close issue
human rejects:
  status:review → status:changes
  with comment
planner/human repairs:
  status:changes → status:ready or status:todo
blocked:
  remove status:in-progress
  add status:blocked
  comment names required human/operator input
```

### AC8: wave semantics

- Master issue tracks dependency map; carries `kind/wave`; MUST NOT carry `dispatch:cell`.
- Each subissue is independently dispatchable only when it has both `dispatch:cell` and `status:todo`.

### AC9: drift handling

If an issue has multiple `status:*` labels, no status label, or illegal status combination:
- Do not claim.
- Report `degraded_reason: dispatch_label_drift` (extend AC14a canonical list at cnos#444).
- Comment with repair instruction.

### AC10: template integration

cnos#450 wake-template cites this protocol and renders:
- `on: issues` labeled trigger
- scheduled sweep trigger
- concurrency group
- dispatch query
- claim/report steps

### AC11: priority deferred

No priority labels in v0. If dependencies matter, do NOT label downstream subissues `status:todo` yet. Deterministic claim order on scheduled sweep: lowest issue number among `dispatch:cell` + `status:todo`.

## Scope

In scope:
- Canonical label set (AC1)
- Claim operation + transitions (AC4, AC7)
- Concurrency discipline (AC5)
- Wave semantics (AC8)
- Drift handling (AC9)
- Citations from cnos#450 + cnos#444 (AC10)
- Skill spec at `cnos:src/packages/cnos.core/skills/agent/dispatch-protocol/SKILL.md`

Out of scope:
- Cell runtime implementation (separate concern; cohere orchestrates; cells run γ/α/β internally).
- Per-org label overrides.
- Priority/queue ordering beyond deterministic-by-issue-number sweep.
- Assignee-as-lock (assignee = human-visible ownership, not dispatch mutex).

## Cycle scope sizing

| Factor | Reading | Splitting? |
|---|---|---|
| (a) New code surface | 1 skill spec + label set definition | No |
| (b) Cross-module breadth | cnos.core/agent + GitHub Actions surface | No |
| (c) Lifecycle span | design + skill text + first-use at cnos#450/cnos#444 implementation | No |
| (d) MCA preconditions | partial; reviewer-converged design captured here | design-and-build |
| (e) Independent shippability | Yes — cnos#450 + cnos#444 implementations cite this | n/a |

Decision: keep whole. design-and-build cycle.

## Proof plan

Invariant: a wake fires, observes an open issue with `dispatch:cell` + `status:todo`, claims it (re-read + label transition + comment), launches a stub cell runtime, returns receipt. Wake on a non-`dispatch:cell` issue does not claim. Wake on a `dispatch:cell` issue without `status:todo` does not claim.

Surface: skill spec + a test issue at cnos.

Oracle: test issue passes through the full label cycle (ready → todo → in-progress → review → close) with one wake claim and one receipt.

## Related artifacts

- cnos#444 (cohere — depends on this for dispatch behavior)
- cnos#450 (wake-template — depends on this for template content)
- cnos#446, #448, #449, #452 (foundation; not directly dependent but ordered before this in merge order)
- `cn-sigma:threads/adhoc/20260616-coherer-console-vs-wake-worker.md` (interface boundary doctrine; this protocol is the concrete dispatch mechanism)

## Non-goals

- Not implementing the cell runtime (that's cohere's orchestration scope).
- Not per-org label customization.
- Not building a kanban UI; GitHub Issues UI is sufficient.

## Closure

Closeable when AC1–AC11 met; test issue at cnos demonstrates full cycle; cnos#450 + cnos#444 implementations cite this protocol explicitly.
````

### Comment to post on cnos#444 (AI2)

````markdown
## Merge order update — cnos#454 inserted

Per Directive 12 (reviewer convergence on bumpt-side learnings): the dispatch protocol is a control-plane prerequisite for both this issue's implementation AND cnos#450's wake-template renderer. Without it, both risk re-inventing incompatible dispatch behavior.

Updated merge order:

```
1. PR #445   Writer Locality + same-repo carve-out
2. cnos#446  activate Tier 1a/1b (amended)
3. cnos#448  cn-sigma containerization (amended)
4. cnos#449  registration + private credentials
5. cnos#452  wake-substrate v0
6. cnos#454  dispatch-protocol  ← INSERTED (D12)
7. cnos#450  wake-template renderer (cites #454)
8. cnos#444  cohere implementation (cites #454; #444 itself cites #454 in its design)

Parallel follow-on:
9. cnos#453  wake-substrate-openai-compat (NIM canary)
```

Implication for AC21/AC22 of this issue (Scenario A/B end-to-end): the wake's "claim and execute" semantics route through cnos#454's protocol. cohere doesn't sweep channels for arbitrary directives — it claims `dispatch:cell + status:todo` issues. Cohere's existing channel-sweep for directives (e.g., the D1–D12 pattern this thread itself uses) is for COHERER-CONSOLE directives, not for worker dispatch.

That distinction is the one the reviewer's last directive named:
- coherer console writes directives that other consoles + planner read
- workers act only from `dispatch:cell + status:todo` issues, not from channel directives

This issue's AC23 (honest "one verb + visible gates") gains one more visible gate: the human's `status:todo` flip is the dispatch authorization.
````

### Cursor

Unchanged (directive-out, no inbound). Closes when AI3 confirms both URLs.
