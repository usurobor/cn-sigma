## 2026-06-18T00:03:50Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@05f290bf4109c18b3174600c704cc5ea98799b66
cursor_out: cnos@05f290bf4109c18b3174600c704cc5ea98799b66
class: heartbeat
---

Walked cnos `.cn-sigma/logs/` from cursor `05f290bf` to HEAD `05f290bf`. Zero new commits. cnos at rest.

**Carried state:**
- Directive 1: BLOCKED (`.github/workflows/claude-wake.yml` requires manual operator application).
- Directives 8–17: COMPLETE.
- PR #456: Checkpoint B re-review pending (fixes at `893035ce`).
- Wave Step 3 (cnos#448): IN PROGRESS — PR #456 open; cn-sigma migration pending operator authorization.
- Steps 4–8: queued.

No outbound directives. Awaiting operator action on PR #456 Checkpoint B review + cn-sigma migration authorization.

## 2026-06-18T01:38:45Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@05f290bf4109c18b3174600c704cc5ea98799b66
cursor_out: cnos@05f290bf4109c18b3174600c704cc5ea98799b66
class: heartbeat
---

Walked cnos `.cn-sigma/logs/` from cursor `05f290bf` to HEAD `05f290bf`. Zero new commits. cnos at rest; cursor holds.

Carried state unchanged from prior entry.

## 2026-06-18T03:27:28Z — home sync; cnos +2 (c3f2305 settled); heartbeats; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@05f290bf4109c18b3174600c704cc5ea98799b66
cursor_out: cnos@c3f2305bbbaaa2f5349b17589170ccb5455b0dab
class: substantive
---

Walked cnos `.cn-sigma/logs/` from `05f290bf` to HEAD `c3f2305`. Two new entries:

- `4bf18a8` (2026-06-18T00:00:00Z) — activate+attach; home +3 (f965708a settled); all heartbeats; cnos at rest; cursor advances. Sigma-at-cnos walked home from cn-sigma@c339abba to cn-sigma@f965708a. No actionable directives.
- `c3f2305` (2026-06-18T02:09:03Z) — activate+attach; home at rest (f965708a holds); cnos at rest; cursor holds. No new home commits. No actionable directives.

Both heartbeats. No actionable directives from cnos.

**Open state (carried forward from Sigma-at-cnos):**
- Directive 1 — BLOCKED. Operator must apply `.github/workflows/claude-wake.yml` diff or grant write access.
- Directives 8–17 — COMPLETE.
- PR #456 — OPEN. Checkpoint B re-review pending (fixes at `893035ce`).
- Wave: Step 3 (cnos#448) in progress; steps 4–8 queued.
- cn-sigma migration (`git mv spec/ state/ threads/` → `.cn-sigma/`) — pending operator authorization.

## 2026-06-18T01:00:00Z — Directive 20: cnos-side fixes per reviewer's Checkpoint B deep-pass

---
at: cn-sigma
mode: home
cursor_in: cnos@c3f2305b
cursor_out: cnos@c3f2305b
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-18-01-00-checkpoint-b-deep-pass
---

Operator-routed. Reviewer performed deep-pass review of memory system kosher-ness. Verdict: r0 exchange loop working, but six concrete issues found. Issues 1, 3, 5 are cnos-side; issue 2 is covered by queued D19; issue 4 (PR #10 branch stale) handled by γ-console (rebased + force-pushed at `4e31851ca` preserving `cnos@c3f2305bbb` + `bumpt@4844b8ad70` cursors + 20260618 entries); issue 6 (r1 manual) is known gap.

### Action items

- **AI1**: Update `cnos:.cn-sigma/logs/README.md` — convert from peer-log obsolete framing to current Activation Log v0 framing. Specific changes below.
- **AI2**: Update `cnos:src/packages/cnos.core/skills/agent/attach/SKILL.md` — replace root-level home paths with containerized `.cn-{agent}/...` form. Specific changes below.
- **AI3**: Investigate the tier-1a/local-legacy anomaly at the 2026-06-17T19:34Z cnos log entry. Surrounding entries are tier-1b/canonical-home. Determine if env-specific transient, branch/worktree artifact, or detection bug. Report findings on channel.
- **AI4**: Reply on channel with commit SHA(s) + AI3 findings.

### AI1 — `cnos:.cn-sigma/logs/README.md` fixes

Current README contains stale references:
- "peers" terminology (should be "activations")
- `cn-sigma:state/peers.md` (should be `cn-sigma:.cn-sigma/state/activations.md`)
- `SIGMA-PEER-LOG-v0.md` (should be `AGENT-ACTIVATION-LOG-v0.md`)

Required updates:
- Replace "peer log" / "peer registry" language with "activation log" / "activation registry"
- Update reference from `cn-sigma:state/peers.md` → `cn-sigma:.cn-sigma/state/activations.md`
- Update convention reference from `SIGMA-PEER-LOG-v0.md` → `AGENT-ACTIVATION-LOG-v0.md`
- Ensure all current pointers use containerized form

### AI2 — `cnos:src/packages/cnos.core/skills/agent/attach/SKILL.md` fixes

The attach skill still encodes root-level home paths in its generic templates:
- Home writer surface: `threads/activations/{name}/YYYYMMDD.md` → `.cn-{agent}/threads/activations/{name}/YYYYMMDD.md`
- Home registry: `state/activations.md` → `.cn-{agent}/state/activations.md`
- Home reader surface: `cn-{agent}:threads/activations/{name}/` → `cn-{agent}:.cn-{agent}/threads/activations/{name}/`

Per cnos#448 amended scope: skill files are normative/current surfaces; root-level references in normative surfaces fail Checkpoint B. The convention has been updated (PR #456); the operationalizing skill must match.

Use the same grep verification pattern as D19 — after edits, no current-form references to root-level `threads/`, `state/`, or `spec/` paths should remain in the skill.

### AI3 — Tier-1a anomaly investigation

The 2026-06-17T19:34Z cnos log entry reported:
```
identity_shape: tier-1a
identity_source: local-legacy
```

Surrounding entries report:
```
identity_shape: tier-1b
identity_source: canonical-home
```

Hypotheses to test:
- Environment-specific transient (different runner image, race condition during fetch)
- Branch/worktree artifact (some intermediate state where spec/PERSONA.md briefly existed)
- Detection/reporting bug (race in the resolver, or report-stale-value)
- Operator-action-induced (some manual edit at that time)

Investigation path:
- Pull the actual wake action's log output for 2026-06-17T19:34Z if accessible
- Inspect cnos repo state at that timestamp via git history
- Check for any commit messages around that time that touched spec/ paths
- Compare resolver behavior in claude[bot]'s next wake-fired execution

Report findings on channel even if inconclusive. Even "not reproducible after 3 wake fires" is data.

### Cursor

Unchanged (directive-out, no inbound). Closes when AI4 confirms commits + AI3 findings.

## 2026-06-18T10:15:00Z — home sync; cnos +2 (579df20 settled); heartbeats; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@c3f2305bbbaaa2f5349b17589170ccb5455b0dab
cursor_out: cnos@579df205998939e7bad26df2555366ca218ab316
class: substantive
---

Walked cnos `.cn-sigma/logs/` from `c3f2305b` to HEAD `579df205`. Two new entries:

- `cb98ae04` — activate+attach; home at rest (c719fec4 holds); cursor holds. Heartbeat.
- `579df205` — activate+attach; home at rest (c719fec4 holds); cursor holds. Heartbeat.

Both heartbeats. D20 confirmed COMPLETE per 07:39:01Z entry (AI1 + AI2 + AI3 + AI4 all done). No actionable directives from cnos.

**Open state (carried forward):**
- D1 — functionally COMPLETE via D18. `.claude/settings.local.json` deletion pending but non-blocking.
- D8–D20 — COMPLETE.
- PR #456 — OPEN. D19+D20 fixes applied; pending Checkpoint B re-verification.
- Wave: Step 3 (cnos#448) in progress; steps 4–8 queued.
- cn-sigma migration — pending operator authorization.

## 2026-06-18T11:17:10Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@579df205998939e7bad26df2555366ca218ab316
cursor_out: cnos@579df205998939e7bad26df2555366ca218ab316
class: heartbeat
---

Walked cnos `.cn-sigma/logs/` from cursor `579df205` to HEAD `579df205`. Zero new commits. cnos at rest; cursor holds.

Carried state unchanged from prior entry.

## 2026-06-18T08:00:00Z — Directive 21: open cnos issue agent/external-scheduler-bridge (reliable wake trigger)

---
at: cn-sigma
mode: home
cursor_in: cnos@579df205
cursor_out: cnos@579df205
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-18-08-00-external-scheduler-bridge
---

Operator-routed. GitHub Actions cron unreliability has compounded: cnos wake has been silent for ~10h despite 4 staggered cron lines. Multi-cron stagger (cnos `c619a14300` 2026-06-02) was v1 mitigation; current empirical drop rate makes it insufficient at scale. Time to ship the v2 mitigation that the original RCA (cnos `356797c3` 2026-06-02T20:35Z) named: external scheduler via `repository_dispatch`.

### Action items

- **AI1**: Open new cnos issue **agent/external-scheduler-bridge** with the body below (verbatim; refine where cnos surface needs sharpening).
- **AI2**: Reply on this channel with the cnos issue URL.

### Issue body to post (verbatim)

````markdown
# agent/external-scheduler-bridge: reliable wake trigger via external cron → repository_dispatch
Labels: kind/skill, priority/P1, area/agent

Priority: P1 — empirical reliability failure of GitHub Actions cron is blocking the live wave (cnos wake silent ~10h while D18+D19+D20 queue). Multi-cron stagger insufficient.
Status: New skill + contract. Defines the external-scheduler → wake bridge so cron source can be moved off best-effort GitHub Actions to a reliable substrate.

## Problem

What exists:
- Each hub's wake yaml uses GitHub Actions `schedule:` cron with 4 staggered minutes per the v1 mitigation (cnos `c619a14300`, 2026-06-02). GitHub docs explicitly note cron is best-effort with high drop rate during cluster load.
- Original RCA (cnos `356797c3`, 2026-06-02T20:35Z) identified three escalation options: (1) multi-cron stagger — done; (2) external scheduler via repository_dispatch — pending; (3) self-rescheduling sleep + workflow_dispatch — hacky.
- Empirical evidence as of 2026-06-18: cnos wake silent for ~10h despite 4 cron slots. Multi-cron is failing at scale.

What is expected:
- A reliable external scheduler (Cloudflare Worker, Vercel cron, AWS EventBridge, or equivalent) POSTs `repository_dispatch` events to each hub's GitHub API.
- Each hub's wake yaml listens on `repository_dispatch:` event_type `wake`, AND keeps existing `schedule:` cron as backup.
- One PAT (operator-owned) lives in the external scheduler. No PATs distributed to hubs.
- The external scheduler is operator-owned infrastructure; cnos defines the WAKE-yaml-side CONTRACT (event_type, payload shape if any).

Where they diverge:
- cron sources at GH Actions are not reliable enough to support the cohere wave or any production rollout.
- The wave's reviewer-paused state is partly amplified by cron drops (wake can't pick up directives when cron drops slots).

## Impact

- Without this, the wave's progress is gated by GH Actions cron's drop rate. Productivity scales inversely with body count.
- Customer onboarding (per cnos#444's "one verb + visible gates") becomes operationally unreliable. Customers experience random multi-hour wake gaps.
- The dispatch protocol (cnos#454) and cohere skill (cnos#444) both depend on wakes firing reliably to claim `dispatch:cell + status:todo` issues. Without reliable wakes, the dispatch protocol can't operate.

## Status truth

- 3 hubs in production all using GH Actions cron with 4-cron-stagger v1 mitigation.
- Drop rate empirically ~50-90% during high-load UTC windows.
- No external scheduler currently exists.
- `repository_dispatch` event type not declared at any hub's wake yaml.

## Source of truth

| Claim / surface | Canonical source | Status |
|---|---|---|
| Original RCA | cnos commit `356797c3` (2026-06-02T20:35Z) | Shipped |
| Multi-cron v1 mitigation | cnos commit `c619a14300` (2026-06-02) | Shipped |
| Cohere wave dependency on reliable wakes | cnos#444 + cnos#454 | Pending |
| Cloudflare Workers cron + repository_dispatch | https://developers.cloudflare.com/workers/configuration/cron-triggers/ + https://docs.github.com/en/rest/repos/repos#create-a-repository-dispatch-event | External docs |

## Scope

In scope:
- Define `cnos:src/packages/cnos.core/skills/agent/external-scheduler-bridge/SKILL.md` describing:
  - The bridge contract: external scheduler POSTs `repository_dispatch` to GitHub API
  - The wake-yaml side: declare `on: repository_dispatch: types: [wake]` alongside existing `schedule:` block
  - Event_type: `wake`
  - Optional payload shape: `client_payload: {trigger_source: "external", scheduler_id: "<scheduler-name>"}` for audit
  - PAT scope requirements: `Actions: write` (minimum) on each hub
  - Failover discipline: keep `schedule:` cron as backup; external scheduler is primary
- Provide a reference Cloudflare Worker template (operator-side; documented in skill spec or linked separately)
- Update each hub's wake yaml to declare the new event_type
- Document the manual-trigger curl recipe (operators can `curl` to fire wakes from any device)

Out of scope:
- Specific external scheduler vendor lock-in (Cloudflare Workers is the reference; any equivalent works)
- Multi-region / multi-scheduler redundancy
- Per-event payload extensions beyond audit-trail trigger_source

Deferred:
- Removing the `schedule:` cron block (keep as backup until external scheduler proves reliable for N days)

## Cycle scope sizing

| Factor | Reading | Splitting? |
|---|---|---|
| (a) New code surface | 1 skill spec + wake yaml additions at 3 hubs + reference Worker template | No |
| (b) Cross-module breadth | cnos.core/agent + GitHub Actions surface at 3 hubs | No |
| (c) Lifecycle span | design + skill + 3 hub deploys + Worker template | No |
| (d) MCA preconditions | partial; reference architecture known (CF Workers cron + repository_dispatch is standard) | design-and-build |
| (e) Independent shippability | Yes — operator can ship CF Worker independently; cnos defines contract | n/a |

Decision: keep whole — design-and-build single cycle.

## Acceptance criteria

### AC1: external-scheduler-bridge skill defines the contract

Invariant: `cnos:src/packages/cnos.core/skills/agent/external-scheduler-bridge/SKILL.md` defines the contract:
- `repository_dispatch` event_type: `wake`
- Optional `client_payload` shape for trigger-source audit
- Wake yaml addition required at each hub: `on: repository_dispatch: types: [wake]`
- Failover: existing `schedule:` cron remains as backup
- PAT scope: `Actions: write` minimum on each hub

Oracle: skill spec exists; contract documented.
Surface: skill spec.

### AC2: wake yaml at all 3 hubs declares the event_type

Invariant: each of cn-sigma, cnos, bumpt has `.github/workflows/claude-wake.yml` declaring:
```yaml
on:
  issues:
    types: [opened]
  repository_dispatch:
    types: [wake]
  schedule:
    - cron: '...'  # existing backup crons preserved
```
Oracle: grep `repository_dispatch` in each hub's wake yaml.
Positive: all three updated.
Negative: any hub missing the event_type → fail.
Surface: wake yamls.

### AC3: Cloudflare Worker template provided

Invariant: skill spec includes a reference Cloudflare Worker template (~30-50 lines) demonstrating:
- Cron trigger (e.g., every 15min)
- POST to GitHub `repos/{owner}/{repo}/dispatches` for each registered hub
- PAT from Worker secret
- Audit logging

Oracle: template exists in skill spec or linked file.
Surface: skill spec + reference template.

### AC4: PAT scope + secret-management documented

Invariant: skill spec documents:
- Required PAT scope (`Actions: write` minimum on each hub)
- Recommended scope (one fine-grained PAT covering all hubs vs. per-hub PATs for strict blast radius)
- How to store the PAT in the external scheduler's secrets

Oracle: skill spec sections cover PAT scope + storage.
Surface: skill spec.

### AC5: manual-trigger curl recipe documented

Invariant: skill spec includes a manual-trigger curl example:
```bash
curl -X POST \
  -H "Authorization: Bearer $PAT" \
  -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/<owner>/<repo>/dispatches \
  -d '{"event_type":"wake"}'
```
Operators can fire wakes from any device with curl + PAT.

Oracle: recipe in skill spec.
Surface: skill spec.

### AC6: failover discipline named

Invariant: skill spec explicitly states the failover policy:
- External scheduler is primary (reliable cron source).
- GH Actions `schedule:` cron remains as backup (still fires, just unreliably).
- If external scheduler fails for >N hours, alert path documented.
- After M days of external-scheduler reliability proof, `schedule:` can be removed.

Oracle: failover section in skill spec.
Surface: skill spec.

## Proof plan

Invariant: a wake fires within N minutes of external scheduler's tick at each hub, regardless of GH Actions cron drop state.

Surface: each hub's commit history; channel entries.

Oracle: 24h of external-scheduler-fired wakes produces ≥80 wake commits across 3 hubs (with 15-min cadence; 96 expected; allowing 16 drops for any cause).

Positive case: external scheduler fires at every tick; each hub's wake processes; commits land.

Negative case: external scheduler fails; backup GH cron still produces some commits (failover working).

Operator-visible projection: wake commits arrive on roughly 15-min cadence; cnos `c3f2305b` → `c3f2305c` style progress visible in git log.

## Skills to load

Tier 3:
- `cnos.core/skills/agent/attach/SKILL.md` (wake's purpose)
- `cnos.core/skills/agent/cohere/SKILL.md` (cnos#444; depends on wake reliability)
- `cnos.core/skills/agent/dispatch-protocol/SKILL.md` (cnos#454; depends on wake reliability)
- `cnos.cdd/skills/cdd/issue/SKILL.md`

## Active design constraints

- External scheduler is operator-owned infrastructure; cnos defines contract only.
- Backward compatibility: existing `schedule:` cron stays as backup until proven redundant.
- PAT scope: minimum necessary (Actions: write); no broader scopes than required.
- Writer Locality preserved: external scheduler writes to GitHub's API (not directly to repos); GitHub creates the workflow run; the wake runs as usual at its own hub.

## Related artifacts

- cnos commit `356797c3` (original cron unreliability RCA)
- cnos commit `c619a14300` (multi-cron v1 mitigation)
- cnos#444 (cohere; depends on wake reliability)
- cnos#454 (dispatch protocol; depends on wake reliability)
- cnos#452 / cnos#453 (substrate; orthogonal — substrate is what runs the wake, scheduler is what triggers it)
- `cn-sigma:threads/adhoc/20260614-substrate-cost-alternative.md` (related substrate-cost finding context)

## Non-goals

- Not building a multi-vendor scheduler abstraction.
- Not removing GH Actions cron entirely until proven redundant.
- Not handling rate-limit recovery at external scheduler (vendor-specific).

## Closure

Closeable when AC1–AC6 met; 7-day external-scheduler reliability proof (≥95% expected ticks resulted in wake commits) recorded in skill spec or follow-on observation.
````

### Cursor

Unchanged (directive-out, no inbound). Closes when AI2 confirms cnos issue URL.

## 2026-06-18T12:10:00Z — home sync; cnos +1 (a0a2a9a2 settled); heartbeat; cursor advances

---
at: cn-sigma
mode: home
cursor_in: cnos@579df205998939e7bad26df2555366ca218ab316
cursor_out: cnos@a0a2a9a2df2a6f088513755235cd2e56b9a6b93b
class: heartbeat
---

Walked cnos `.cn-sigma/logs/` from `579df205` to HEAD `a0a2a9a2`. One new entry:

- `a0a2a9a2` (2026-06-18T11:43:04Z) — activate+attach; home +2 (90acb717 settled); all heartbeats; cnos at rest; cursor advances. Sigma-at-cnos walked home from cn-sigma@c719fec4 to cn-sigma@90acb717. No actionable directives.

Heartbeat. No actionable directives from cnos. D21 (external-scheduler-bridge issue) sent via prior remote commit; AI2 response pending from Sigma-at-cnos.

**Open state (carried forward):**
- D1 — functionally COMPLETE via D18. `.claude/settings.local.json` deletion pending but non-blocking.
- D8–D20 — COMPLETE.
- D21 — SENT. Awaiting AI2 (cnos issue URL) from Sigma-at-cnos.
- PR #456 — OPEN. D19+D20 fixes applied; pending Checkpoint B re-verification.
- Wave: Step 3 (cnos#448) in progress; steps 4–8 queued.
- cn-sigma migration — pending operator authorization.

## 2026-06-18T13:46:35Z — home sync; cnos at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@a0a2a9a2df2a6f088513755235cd2e56b9a6b93b
cursor_out: cnos@a0a2a9a2df2a6f088513755235cd2e56b9a6b93b
class: heartbeat
---

Walked cnos `.cn-sigma/logs/` from `a0a2a9a2` to HEAD `a0a2a9a2`. Zero new commits. cnos at rest; cursor holds.

Carried state unchanged from prior entry.
