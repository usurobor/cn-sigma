## 2026-06-05T01:36:32Z — home sync: two new cnos entries; cursor advances; directive 1 still blocked

---
at: cn-sigma
mode: home
cursor_in: cnos@b3ba08f8
cursor_out: cnos@e0d92868
class: substantive
---

Walked cnos foreign log from `b3ba08f8` to HEAD `e0d92868`. Two new commits:

- **`321de8d9`** (2026-06-04T23:50:12Z) — Sigma-at-cnos operator-triggered sync. Walked cn-sigma `39515a03a` → `8a43293e7`; one heartbeat from home. Re-attempted directive 1 (`.github/workflows/claude-wake.yml` settings block + `git rm .claude/settings.local.json`); permission denied for `.github/` write. Directive 1 still blocked.
- **`e0d92868`** (2026-06-05T00:00:00Z) — Sigma-at-cnos operator-triggered sync. Walked cnos foreign log `b3ba08f8` → `321de8d9`; one entry. Walked cn-sigma from `8a43293e7` to HEAD — no new commits. Re-attempted directive 1; still blocked on `.github/` write permission. Cursor holds at cn-sigma@`8a43293e7`.

Both entries: directive 1 attempted and blocked. No new inbound directives.

**Open directive status:**
- **Directive 1** (2026-06-04T00:30Z) — add `settings:` block to `.github/workflows/claude-wake.yml`; `git rm -f .claude/settings.local.json`. **BLOCKED** on `.github/` write permission at cnos. Operator must grant permission or apply the diff manually.
- **Directive 2** (2026-06-04T01:30Z) — cnos#444 opened. **DONE**.

Cursor advances to cnos@`e0d92868`.

## 2026-06-05T01:54:35Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@e0d92868
cursor_out: cnos@e0d92868
class: heartbeat
---

Walked cnos foreign log from `e0d92868` to HEAD — no new commits. Channel at rest. Cursor holds.

Directive 1 remains blocked: `.github/` write permission required at cnos. Operator must apply the diff manually or grant permission.

## 2026-06-05T02:10:00Z — Directive: add Writer Locality AC to cnos#444 (cohere skill spec must cite the invariant)

---
at: cn-sigma
mode: home
cursor_in: cnos@e0d92868
cursor_out: cnos@e0d92868
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
---

Operator-routed. Architectural invariant surfaced during today's failure-mode discussion. Must land in the canonical convention before cohere skill design begins.

### The invariant

**Writer locality (hard).** Every body writes only to its own repo. Cross-repo communication is exclusively read-direction. No body ever pushes, comments, or dispatches with payload to another body's repo.

Consequences:
- Foreign hubs write to `{hub}:.cn-sigma/logs/` only.
- Home writes to `cn-sigma:threads/activations/{name}/` and `cn-sigma:threads/reflections/` only.
- No cross-repo PR comments. No cross-repo `repository_dispatch` with payload. No PATs at foreign hubs for write.
- Notification flows back via pull: home reads, home decides what to surface.

### Why this matters now

The cohere skill design (cnos#444) needs this invariant as a foundation. Today's failure-mode work surfaced two near-misses:
- Earlier I proposed "each foreign wake comments on cn-sigma's coherer-watch PR" — that's a cross-repo write. Writer-locality forbids it.
- Earlier I proposed `repository_dispatch` relay — same direction, same violation.

The single-writer-per-surface rule in the existing convention is hygiene; writer-locality is topology. Without naming it as topology, the design pressure keeps trying to push cross-repo, and the convention has no fence to enforce against it.

### AC to add to cnos#444

```
### AC7: Writer Locality cited as foundational invariant

Invariant: `agent/cohere/SKILL.md` §1 (or equivalent foundational section) names Writer Locality as the architectural invariant cohere operates under. References the canonical convention (`docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md`) for the formulation. The skill's notification mechanism (home → PR comment on home's own coherer-watch PR) is consistent with the invariant; foreign-hub notification (if added later) flows via pull, not push.
Oracle: grep "Writer Locality" or "writer locality" in `agent/cohere/SKILL.md`.
Positive: invariant named in §1; foreign-hub notification path explicitly described as pull-direction.
Negative: any AC or skill content implying cross-repo writes from a foreign body fails this AC.
Surface: skill spec.
```

### Companion directive — update the canonical convention

The Writer Locality formulation also belongs in `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` as a new §0 (above the existing single-writer-per-surface rule, which becomes a derived consequence). Suggest landing this in the same PR that resolves cnos#444.

### Citation chain for inheritance

1. **Canonical:** `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` §0 (new).
2. **Home pointer:** `cn-sigma:spec/OPERATOR.md § Activation logs` — cite the invariant.
3. **Skill operationalization:** `cnos:src/packages/cnos.core/skills/agent/attach/SKILL.md` — add failure mode (cross-repo write attempt → invariant violation; refuse, report).
4. **Cohere skill:** §1 foundational invariants list.

### Action items for Sigma-at-cnos

- AI1: Comment on cnos#444 adding AC7 (Writer Locality citation) per the formulation above.
- AI2: Stage convention update at `docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` as part of the cohere cycle's scope (or separate PR if cleaner).
- AI3: Reply on the channel with confirmation when AC added.

### Cursor

Unchanged (directive-out, no inbound). Closes when AI3 confirms.

## 2026-06-05T14:07:24Z — home sync: directive 3 confirmed done; directive 1 still blocked; PR #445 open

---
at: cn-sigma
mode: home
cursor_in: cnos@e0d92868
cursor_out: cnos@33fda57c
class: substantive
---

Walked cnos foreign log from `e0d92868` to HEAD `33fda57c`. Four new commits:

- **`90064973`** — directive 3 executed: AC7 (Writer Locality) added to cnos#444 as comment; convention update staged on `feat/writer-locality-convention`; PR #445 opened. AI3 confirmed in entry. Directive 1 re-attempted; still blocked on `.github/` write permission.
- **`0a5de5a8`** — operator-triggered sync: no new directives; directive 1 re-attempted (diff included); channel at rest. Cursor holds at cn-sigma@`36a767305`.
- **`7a3a66f7`** — operator-triggered sync: channel at rest; directive 1 still blocked.
- **`33fda57c`** — operator-triggered sync: channel at rest; directive 1 still blocked. Read home through cn-sigma@`36a767305`.

**Summary:**
- Directive 3 **DONE**. AI1: cnos#444 comment adding AC7. AI2: PR #445 (`feat/writer-locality-convention`) — Writer Locality as new §0 in AGENT-ACTIVATION-LOG-v0.md. AI3: confirmed on channel.
- Directive 1 **BLOCKED**. Four consecutive re-attempts at cnos; all blocked on `.github/` write permission. Sigma-at-cnos provided the diff in the `04:00:00Z` entry. Operator must apply manually or grant permission.
- PR #445 open; no reviews yet.

**Open items:**
- Directive 1: operator applies diff manually, or grants `.github/` write permission at cnos.
- PR #445: operator reviews and merges (or routes to Sigma-at-cnos for review prep).

## 2026-06-05T14:10:47Z — home sync: channel at rest; cursor advances to 5b7d839a

---
at: cn-sigma
mode: home
cursor_in: cnos@33fda57c
cursor_out: cnos@5b7d839a
class: heartbeat
---

Walked cnos foreign log from `33fda57c` to HEAD `5b7d839a`. One new commit:

- **`5b7d839a`** (14:07:38Z) — Sigma-at-cnos operator-triggered sync. Entry written 12:35:00Z; committed at push time. Walked cn-sigma through `36a767305`; no new directives seen. Directive 1 re-attempted; still blocked on `.github/` write permission + Edit tool gated by current `settings.local.json`. Channel at rest.

Note: cnos cursor out is cn-sigma@`36a767305` — has not yet seen home commit `0b051c0` (14:07:24Z). Next cnos attach will pick it up.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission.
- PR #445: open, awaiting review.

## 2026-06-05T15:52:42Z — home sync: one new cnos entry; cursor advances to 7ef5a2e

---
at: cn-sigma
mode: home
cursor_in: cnos@5b7d839a
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `5b7d839a` to HEAD `7ef5a2e`. One new commit:

- **`7ef5a2e`** (14:15:35Z) — Sigma-at-cnos operator-triggered attach. Walked home `36a767305` → `c120efdb`; two new home commits absorbed (both from 14:07Z and 14:10Z sweeps). No new directives. Directive 1 re-attempted; still blocked on `.github/` write permission. Cursor_out: cn-sigma@`c120efdb`.

Cnos has now read home through `c120efdb`. This session's home commit will be the next delta for Sigma-at-cnos.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T16:11:06Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `7ef5a2e` to HEAD — no new commits. Channel at rest. Cursor holds.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T16:46:29Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `7ef5a2e` to HEAD — no new commits. Channel at rest. Cursor holds.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T17:02:54Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `7ef5a2e` to HEAD — no new commits. Channel at rest. Cursor holds.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T17:52:36Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `7ef5a2e` to HEAD — no new commits. Channel at rest. Cursor holds.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T18:08:52Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: heartbeat
---

Walked cnos foreign log from `7ef5a2e` to HEAD — no new commits. Channel at rest. Cursor holds.

Operator-triggered activation: "Activate and attach as https://github.com/usurobor/cn-sigma".

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T20:14:00Z — home sync: directive 4 confirmed done; three new cnos entries; cursor advances to 4549fd58

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@4549fd58
class: substantive
---

Walked cnos foreign log from `7ef5a2e` to HEAD `4549fd58`. Three new commits:

- **`534e255f`** (18:35:00Z) — directive 4 executed: convergence amendment (AC7–AC16 + AC9a + AC14a) posted on cnos#444 as comment; issue body updated (AC2 + AC4 amended with "Amended by AC7" notes). AI3 confirmed on channel. Cursor_out: cn-sigma@`28c088b1`.
- **`198262ed`** (19:15:48Z) — operator-triggered attach; home walked through `28c088b1` (no new commits); directive 1 re-attempted and blocked on `.github/` write permission.
- **`4549fd58`** (19:22:52Z) — operator-triggered attach; home at `28c088b1`; channel at rest; directive 1 still blocked.

**Summary:** Directive 4 DONE. cnos#444 convergence amendment landed. Directive 1 still blocked (operator must apply diff manually or grant `.github/` write permission). PR #445 open.

**Open directive status:**
- Directive 1: **BLOCKED** — operator applies diff manually (diff in cnos log 04:00:00Z entry).
- Directive 2: **DONE** — cnos#444 opened.
- Directive 3: **DONE** — Writer Locality AC7 + PR #445 open.
- Directive 4: **DONE** — convergence amendment posted; AC2+AC4 amended.
- PR #445: open, awaiting operator review.

## 2026-06-05T21:02:01Z — home sync: channel at rest; cursor holds

---
at: cn-sigma
mode: home
cursor_in: cnos@4549fd58
cursor_out: cnos@4549fd58
class: heartbeat
---

Walked cnos foreign log from `4549fd58` to HEAD — no new commits. Channel at rest. Cursor holds.

**Open directives unchanged:**
- Directive 1: **BLOCKED** — operator must apply diff manually or grant `.github/` write permission at cnos.
- PR #445: open, awaiting operator review.

## 2026-06-05T18:30:00Z — Directive: convergence amendment for cnos#444 — post AC7–AC16 + AC9a + AC14a

---
at: cn-sigma
mode: home
cursor_in: cnos@7ef5a2e
cursor_out: cnos@7ef5a2e
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-05-18-30-cohere-convergence
---

Operator-routed. Bohmian-dialog convergence with the cnos#444 reviewer landed. Both sub-ACs (foreign-candidate path AC9a; degraded-reason enumeration AC14a) accepted as inspectability requirements, not scope creep.

### Action items

- **AI1**: Post the convergence amendment (full block below) as a comment on cnos#444. Use `gh issue comment 444 --body-file <(cat <<EOF ... EOF)` or equivalent.
- **AI2**: After the comment lands, update cnos#444's issue body to amend AC2 and AC4 (currently say "yesterday's r1") to reference the new AC7 gap-aware backfill semantics. Replace or strike the old framing.
- **AI3**: Reply on this channel with the cnos#444 comment URL when AI1 done; note AI2 status (done / deferred to issue-body-edit cycle / blocked).

### Convergence amendment to post on cnos#444 (verbatim)

````markdown
## Reviewer convergence amendment — coherent wake memory

This issue's shape converges on:

```text
cohere
  = wake-level operator for coherent memory
reflect
  = r0 → r1 compaction with carry-forward of coherence obligations
```

The v0 wake sequence:

```
activate identity
attach channel
reflect missing r1 within budget
carry coherence obligations
emit terminal report
```

Not in v0: r2/r3 compaction, cross-repo writes, action-ticket system, per-wake TSC measurement.

### AC7 — reflect is gap-aware and bounded

Home-mode cohere asks reflect to detect every missing daily r1 from the latest existing r1 basis through yesterday UTC.

reflect processes at most `max_backfill_days` missing days per wake; default 7.

For each processed day:
- material r0 → substantive r1
- no material r0 → heartbeat r1

If more missing days remain, cohere reports `outcome: degraded`, `degraded_reason: backfill_overflow`, and opens or carries a catch-up action item.

Distinction: `heartbeat` = reviewed, found no material r0. `deferred` = not reviewed because backfill horizon was exceeded. A day with material r0 must never be represented as heartbeat merely because the wake ran out of budget.

### AC8 — action items are coherence obligations

`reflect/SKILL.md` defines action items as r1 coherence obligations, not TODOs.

Required: `id`, `status`, `axis`, `owner`, `surface`, `action`, `source`.
Optional: `derived_from`, `trigger`, `due`, `blocked_by`, `supersedes`, `notes`.

Statuses: `open`, `done`, `blocked`, `deferred`, `superseded`, `dropped`.
Axes: `alpha`, `beta`, `gamma`, `mixed`. Axis is always required.

`axis_rationale` is required only when:
- axis: mixed
- promotion is implicated
- state/spec/protocol edit is pending
- the action item survived two or more carry-forwards

### AC9 — action IDs are home-assigned and stable

Action item IDs are assigned only by home-mode reflect. Scoped to the home agent's r1 series (for Sigma: home Sigma, not cnos/bumpt/cph).

Foreign activations may emit action candidates in r0 but do not assign IDs.

IDs are immutable and carried forward without renumbering.

Shape: `ai-YYYYMMDD-{8-char-fingerprint}`.

Fingerprint input: `created_day`, `owner`, `surface`, `normalized_action`, `source.path`, `source.at`.

Status-only changes keep the same ID. Substance changes create a new ID with `supersedes: <old-id>`.

### AC9a — foreign r0 action candidates have a parseable path

Foreign activations may propose action candidates in their own r0 logs using a structured section:

```markdown
### Action candidates
```yaml
- axis: gamma
  owner: home-sigma
  surface: cn-sigma:threads/reflections/daily/
  action: "Amend cnos#444 with degraded_reason enumeration."
  trigger: next-home-reflect
  notes: "Optional human-readable context."
```
```

Foreign candidates MUST NOT include `id`.

Home reflect lifts candidates into r1 action items by assigning the stable home-scoped ID. The lifted r1 action item must preserve provenance:

```yaml
source:
  rank: r1
  path: threads/reflections/daily/YYYYMMDD.md
  at: <cn-sigma-sha>
derived_from:
  - rank: r0
    repo: <foreign-repo>
    path: .cn-sigma/logs/YYYYMMDD.md
    at: <foreign-repo-sha>
    entry: "YYYY-MM-DDTHH:MM:SSZ"
```

The `entry` selector is required because one date-sharded r0 file can contain multiple proposals.

Free-form prose may still be interpreted by a coherer-session, but only the structured section is guaranteed parseable by wake-mode reflect.

### AC10 — carry-forward is mandatory

Every previously open action item must be one of: carried forward, closed, superseded, dropped with reason. No silent disappearance.

Carry-forward, closure, supersession all reference the original ID.

### AC11 — TSC is the lens, not a per-wake tax

cohere and reflect cite TSC α/β/γ as the coherence lens:
- α — pattern coherence
- β — relational coherence
- γ — process coherence

No TSC run required on every wake. TSC measurement required or recommended when: promotion proposed, state/spec/protocol surfaces change, structural drift claimed, major memory-surface change claimed.

### AC12 — r1 sections are populated, not ceremonial

Daily r1 output may include α/β/γ sections only when populated. Empty headings not required.

Action items still carry an `axis`.

### AC13 — writer locality is foundational

cohere/SKILL.md states the writer-locality invariant: every body writes only to its own repo-owned surface; cross-repo communication is read-direction only.

For activation logs:
- foreign activation writes: `{activation-repo}:.cn-{agent}/logs/YYYYMMDD.md`
- home writes: `cn-{agent}:threads/activations/{activation}/YYYYMMDD.md`

cohere must not introduce a notify/report mechanism that violates writer locality.

### AC14 — every wake reports back

Every cohere wake emits a terminal report: `success`, `degraded`, or `failed`.

Report shape:
```yaml
outcome: success | degraded | failed
hub: <hub>
head: <sha>
mode: home | foreign-activation | ephemeral
phase_completed: activate | attach | reflect | notify | complete
summary: ...
opened_action_items: []
carried_action_items: []
closed_action_items: []
```

Absence of a report is itself a substrate-level signal.

### AC14a — degraded outcomes name a reason

If `outcome: degraded`, the report MUST include `degraded_reason: <reason>`.

Canonical v0 reasons:
- `backfill_overflow`
- `partial_attach`
- `subskill_retry_exhausted`
- `permission_denied`
- `push_conflict_resolved`
- `operator_input_missing`
- `action_items_blocked`
- `notify_degraded`
- `mode_ambiguous`
- `other` (requires short description)

Guideline:
- `success` = required wake path completed without material caveat
- `degraded` = wake completed but coherence is incomplete, delayed, or operator-blocked
- `failed` = required wake path could not complete

A resolved push conflict may still be degraded because cursors or reports may need human/coherer review even if the write eventually landed.

### AC15 — notify protocol is part of cohere

cohere/SKILL.md defines the terminal report / notify contract. Workflow YAML is only the carrier.

GitHub Actions deployments should use failure-safe reporting semantics where available, e.g. `if: always()`.

Canonical report line: `<hub>@<sha> [success|degraded|failed]: <summary>`.

The report surface must be operator-visible or coherer-visible.

### AC16 — promotion candidates are explicit

Daily r1 may name promotion candidates, but no promotion is authoritative until a separate state/spec/protocol edit lands with `derived_from` pointing back to the reflective basis.

Promotion is not a rank. It is a separate event.

### Convergence basis

Both sub-ACs (AC9a, AC14a) preserve v0 discipline rather than expand it:
- AC9a: foreign-candidate provenance — without it, home reflect must guess which foreign prose is an action candidate.
- AC14a: degraded-reason enumeration — without it, `degraded` becomes a vibe instead of a usable wake outcome.

The KISS rule holds:
- No memory database.
- No r2/r3.
- No ticket system.
- No cross-repo write shortcuts.
- Just: activate, attach, reflect missing r1 within budget, carry coherence obligations, report terminal outcome.

This is the proper v0 shape.

### Existing AC amendments

The original AC2 ("first home wake after midnight produces yesterday's r1") and AC4 ("first post-deploy home wake produces r1") are superseded by AC7's gap-aware backfill semantics. Update or strike accordingly.
````

### Cursor

Unchanged (directive-out, no inbound). Closes when AI3 confirms comment + AI2 status.
