# Daily — 2026-04-30

## What shipped

### Cycle #287 complete
- **3.62.0 tagged and pushed** — release CI running
- `cycle/287` branch swept
- γ-closeout, PRA, α/β close-outs all on main
- Issue: γ creates the cycle branch — α and β only check out `cycle/{N}`

### CDD skill patches
- **Incremental write discipline** (`70ff2b1b`) — α self-coherence.md written in 6 named sections (separate commits), β beta-review.md written per review pass (contract → implementation → verdict). Fixes stream idle timeout that killed α mid-generation.
- **#304 merged** (`a8e67b7`) — issue skill v2 + review skill split into `review/contract/SKILL.md` and `review/implementation/SKILL.md`

### CI consolidation
- Merged `coherence.yml` into `build.yml` — single workflow, 6 jobs
- Merged `release-smoke.yml` into `release.yml` — single release workflow with smoke job + `workflow_dispatch` smoke-only mode
- Renamed kata jobs → `binary-verify` / `package-verify` (kata reserved for LLM method harness)
- CI badge and notify messages updated

### README doctrine framing (#306)
- Coherent-agent section rewritten: boundary-preserving, evidence-attached, inspectable agency
- Doctrine added to layer diagram, concept table, Further Reading
- docs/README.md doctrine reading route added
- #306 filed and closed same session

### Issue maintenance
- **#303 patched** — status truth section, Tier 3 skills, AC13 decided (CI runs fixtures)
- **#305 rewritten** — extend `cdd-verify` (not new command), title changed, false premise corrected
- **#245 comment** — LLM-judge stays out of CI, offline eval only

## Key decisions
- **No LLM in CI** — kata-judge is offline eval harness only. CI gates on deterministic checks.
- **Two workflows** — `build.yml` (push/PR) and `release.yml` (tag push). Different triggers, different lifecycles.
- **Incremental write discipline** — MCA for stream timeout, not prompt workaround. Skill is the constraint.
- **Direct-to-main pushes** — 8 commits pushed directly. Acknowledged violation of no-self-merge spirit. Need to decide policy for operator-directed small changes.

## Open items
- Release CI for 3.62.0 in progress
- Hub memory for #287 cycle (γ projected in PRA/closeout but not written to hub)
- #304 follow-ups: issue kata migration to cnos.cdd.kata, phase 2 review split
- γ dispatch ready for #301, #303, #305 (parallel)
- Next MCA: cn dispatch infrastructure (#295 precondition for #286) or #273 (rebase-collision guard)
