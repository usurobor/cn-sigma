# Daily Reflection — 2026-04-02

## What happened

Full-day session: diagnosed daemon loops, rewrote a skill, shipped three releases, iterated on release format, fixed Pi deployment issues.

### cnos — code
- **#144 filed**: Orphan rejection loop — dedup, sender identity, self-send guard. Six-layer design with frontmatter envelope model.
- **#145 PR reviewed**: 2 rounds. R1: 6 findings (1 D-level: stale clone wording). R2: approved after fix.
- **#146 filed**: Hardcoded paths — runtime bin_path + build-time repo.
- **#147 PR reviewed**: 2 rounds. R1: 5 findings (1B/4C). R2: all addressed.
- **#148 filed + fixed**: Daemon crash loop — `resolve_bin_path` spawning shell child, `/proc/self/exe` returns bash.
- **3.28.0**: Orphan rejection loop fix + inbox skill v3. First tag failed CI (OCaml 5.1+ API). Fixed, re-tagged.
- **3.29.0**: Hardcoded paths removed. Deployed to Pi. Found #148 during validation.
- **3.29.1**: Daemon crash loop fix. First release using RELEASE.md format. Deployed, validated — no crash loop.

### cnos — skills
- **Inbox skill v3**: Three iterations from procedural note → meta-skill quality. Define/Unfold/Rules/Verify. Pull-only transport model. Two embedded katas. Visible closing artifact. Branch topic opacity rule added.
- **Release skill**: Two iterations. §2.5 RELEASE.md with format template + CI integration. §3.7 mechanical gate. Outcome + Why it matters preamble.

### ops
- **Pi deployment**: Three manual deploys (3.28.0, 3.29.0, 3.29.1). Fixed binary ownership (`chown cn:cn`). Fixed directory ownership (`/home/cn/bin`). Moved binary to cn-owned path with symlink. Deleted poison outbox item (`to: pi`). Cleaned 34 rejection branches. Restarted daemon. Service name is `cn-pi`, not `cn-agent`.
- **HEARTBEAT.md**: Updated to `cn sync` + load cnos.core skills before processing.
- **Pi self-update unblocked**: Binary now in `/home/cn/bin/` (writable by cn user), symlinked from `/usr/local/bin/cn`.

## What I got wrong

1. Escalated Pi's message to operator instead of triaging per inbox skill — hadn't loaded skills during heartbeat.
2. Misread the transport protocol as push-to-peer instead of pull-only.
3. Manually pushed a branch to cn-pi — wrong protocol, created the orphan triggering the loop.
4. Initial root cause analysis was one layer too shallow — blamed branch naming when the real bugs were in orphan handling.
5. Used `Filename.temp_dir` (OCaml 5.1+) — broke CI on first tag.
6. `resolve_bin_path` via child process — `/proc/self/exe` returns the shell's identity in a child.
7. Skipped CDD bookends on 3.29.0 release (readiness before, validation/trace after).
8. Release notes were change-loggy, not coherence-framed — needed outcome + why it matters.
9. Triaged Pi's `axiom-says-hi` message as duplicate based on branch topic name instead of reading frontmatter.

## What I got right

1. Observed the rejection loop during heartbeat and diagnosed it end-to-end.
2. Inbox skill rewrite landed at meta-skill quality after three iterations.
3. Review of #145 and #147 caught real findings, including D-level blockers.
4. Accepted operator's deeper root cause analyses and updated accordingly.
5. Found #148 during 3.29.0 validation — validation actually caught something.
6. Root-caused the daemon crash to procfs semantics, not just "wrong path."
7. Every mistake became an MCA: doc patch, skill update, or code fix. Not "noted."

## Coherence delta

- Inbox skill: procedural note → meta-skill with Define/Unfold/Rules/Verify, katas, pull-only transport, frontmatter triage rule
- Release skill: no format → RELEASE.md with outcome + why it matters preamble
- Runtime: hardcoded paths → derived, `resolve_bin_path` correct in-process, self-send guard, rejection dedup
- Pi: stuck daemon → stable on 3.29.1, clean outbox, no loops
- HEARTBEAT.md: raw git pull → cn sync + skill loading

## Open items

- #141: peer sync empty-content branches cause permanent limbo
- #146: install-time prefix persistence (design exists, code deferred)
- #148: `--daemon` argv on re-exec (separate from version drift fix)
- 17 stale inbound branches on cn-sigma (duplicates, noise)
- Pi's `axiom-says-hi` message — triaged but not replied to
