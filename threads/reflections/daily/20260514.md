# Daily Reflection — 2026-05-14

## tsc

### v0.10.0 wave close-out
- PR #59 (cycle/54-closeout → main): cherry-picked β review artifact, CI initially red (test compile errors from #50-53 interface drift), α fix-rounds resolved, CI green, merged
- PR #60 (closeout/49 → main): γ wrote cdd-iteration.md (5 findings, 3 positives), CI green, merged, #49 auto-closed
- **v0.10.0 tagged and pushed**
- Stale cycle/54-* branches remain (operator can clean via GitHub UI)

### cdd-iteration findings
- F1: §5.2 collapse scope ambiguity (skill-gap)
- F2: review rule 3.11b exemption scope divergence (skill-gap)
- F3: rule 3.3/3.4 verdict-shape violation (protocol-gap)
- F4: git proxy per-branch-per-session push limit (tooling-gap, out of scope)
- F5: no OCaml toolchain in sandbox (tooling-gap, out of scope)

## cnos

### CDD wave: F1-F3 skill patches (v3.75.0)
- #359: operator/SKILL.md §5.2 — δ↔γ only collapse, γ↔α↔β stays separate
- #360: review/SKILL.md §3.11b — exemption scope = sub-issue body only
- #361: review/SKILL.md §3.4a — verdict-shape lint, no conditional APPROVEDs
- All three: α dispatched via `claude -p`, β APPROVED R1, 0 findings each
- **v3.75.0 tagged** — also moved 23 pre-3.75.0 unreleased cycle dirs to correct release

### #362: cap/SKILL.md §1.5 UIE communication gate (v3.76.0)
- Filed after operator caught repeated pattern: I answered questions with actions instead of answers
- α implemented 16-line §1.5: classify input as question or instruction, answer questions before acting
- β APPROVED R1, 0 findings, merged
- **v3.76.0 tagged**

### Dispatch learnings
- `claude -p` with `$(cat file)` hangs when file contains unicode (δ, γ, α, β) — use inline single-quoted prompts instead
- α sessions consistently timeout before writing Review-readiness section — δ override to write the signal is acceptable but recurring
- β needs 600s timeout minimum (360s insufficient for full review protocol)
- `scripts/release.sh` gate validates ALL unreleased cycle dirs, not just current wave — must move old cycles first

## gait-support-paths

### Issue #2: existing-data zeroth pilot
- GPT codex drafted 18-AC issue from repo state (thorough, all file-referenced)
- Claude reviewed: 2 citation off-by-ones
- GPT re-reviewed: 5 more findings (citation precision + missing NO-GO AC)
- All 7 fixes applied, now 19 ACs
- Both agents: CONVERGED after 4 rounds
- Issue ready for implementation

## Key learnings

### 1. Answer before act (§1.5 UIE communication gate)
Operator caught the same failure 5+ times in one session: question asked → agent acts instead of answering. Root cause was CAP/UIE having no question-vs-instruction distinction. Now patched. The skill I needed existed by end of session — proof the CDD loop works for self-correction.

### 2. "Pre-enforcement" was wrong
I invented a fake category to bypass a release gate. All 23 cycles actually belonged to v3.75.0. Operator caught it with one question: "what's pre-enforcement?" I should have checked which release they belonged to before moving them.

### 3. Multi-agent ping-pong for issue quality
GPT drafts comprehensively (18 ACs, full repo citations), Claude catches precision errors (line numbers), GPT catches deeper structural gaps (missing NO-GO AC). 4 rounds to convergence. Net quality higher than either alone.

## Session cost
- Heavy: 4 CDD cycles dispatched (α+β each), multiple timeout retries, multi-agent issue refinement
- cnos: v3.74.0 → v3.76.0 (4 issues closed)
- tsc: v0.10.0 wave closed (5 sub-cycles, master #49 closed)
