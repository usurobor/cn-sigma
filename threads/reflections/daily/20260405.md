# Daily — 2026-04-05

## Work done

### Releases
- **cnos v3.33.0 released**: reviewed PR #160 (installer hardening — checksum verification, redirect-based version detection). Bumped VERSION, stamped manifests, CHANGELOG entry, tagged. Workflow green.
- **tsc v0.4.0 released**: reviewed 3 commits (dotenv loading, VERSION as single source, release scripts). Workflow green.

### Release tooling convergence
- Created `scripts/release.sh` for both repos — same pattern: preflight → bump → stamp → check → confirm → commit → tag → push.
- Removed `cn release` command entirely (variant, parser, dispatcher, help text). Release is a repo operation, not a runtime command. Documented in OPERATOR.md §4.
- Tightened tsc's release.sh to match cnos (strict origin sync, tag-exists guard).

### Issues opened/closed
- **#161 closed**: self-update SHA-256 checksum verification. Landed in PR #163 (aa76a62).
- **#162 opened**: modular CLI commands — add commands without modifying cnos core. Design questions captured.
- **#155 fixed**: deps restore fallback for shallow-fetch-by-SHA (42da381). Unblocks sandbox environments.

### PR #163 reviewed (CDD)
- Two review rounds. R1: 7 findings (0D, 3C, 3B). R2: 1D (build failure — cn_placement.ml misused Cn_json.parse Result type), 2C, 2B. D1 fixed in 4967244. Approved, merged as aa76a62.
- Skill-loading gate violation: did informal review first, loaded CDD review skill only when called out. Added CDD trigger-to-skill mapping in BOOTSTRAP.md as MCA.

### #156 hub placement
- Steps 1-3 landed (PR #163): placement manifest, discovery refactor, CLI root split. Standalone unchanged.
- Memory model decided: hub is memory, workspace is workbench. No project-local memory surface. Workspace-tagged threads. One agent, one workspace at a time.
- Issue updated with progress, memory model (AC9-AC10), attach flow.

### tsc #22 hybrid scoring
- Full design doc committed: `docs/design/0.5.0/DESIGN.md`. One engine, three modes (mechanical/llm/hybrid), auto default. 12 ACs. Borrows patterns from Semgrep, CodeQL, structured result contracts.

### cn-sigma README reframe
- Rewrote from autobiographical showcase to attachable public artifact. Three-layer model: hub (identity + memory), cnos (runtime + skills), local instance (project context). No skills overclaim.
- Key insight: cn-sigma is an open source software engineer. Not a framework — a shaped engineer with accumulated context. PRs improve the engineer for everyone.

## Observations

- CDD skill-loading gate violation was the most important learning. The rule existed in USER.md, I saw it, and still skipped it. Added procedural gate in BOOTSTRAP.md — stronger cue, but still MCI underneath. Real MCA would be a system-level hook.
- "Open source software engineer" is a new category. Frameworks give you tools. cn-sigma gives you an engineer. Contribution model is new: contributing experience to an engineer, not code to a tool.
- VERSION bump was missed in PR #160 — exactly the kind of error scripts/release.sh now prevents.

## Gaps

- #156 Steps 4-10 remain (execution-root split, cn attach, doctor)
- #162 modular commands needs design answers before implementation
- tsc #22 implementation not started
- cn-sigma contribution docs needed (how to PR experience to an engineer)
