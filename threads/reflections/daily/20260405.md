# Daily — 2026-04-05

## Work done

- **cnos v3.33.0 released**: reviewed PR #160 (installer hardening — checksum verification, redirect-based version detection, curated RELEASE.md). Bumped VERSION, stamped manifests, added CHANGELOG entry, tagged and pushed. Release workflow green.
- **cnos #161 opened**: self-update should verify SHA-256 checksum before replacing binary (parity with installer)
- **tsc v0.4.0 released**: reviewed 3 commits (dotenv loading, VERSION as single source, release scripts). Ran `scripts/release.sh 0.4.0 "feat: dotenv loading"`. Workflow green.
- Populated LLM credentials in tsc `.tsc/.env` from OpenClaw auth config. Moved from wrong path (root `.env`) to correct path before any commit.

## Observations

- VERSION file wasn't bumped in PR #160 — the release script/stamp pattern catches this, but only if you use it. The gap is: nothing in CI enforces VERSION matches the tag being created. Could add a CI check.
- tsc's release.sh is clean: preflight → bump → stamp → check → commit → tag → push. One command, interactive confirm. Good pattern.
- cnos release was still manual (7 individual commands). tsc's `release.sh` wraps the same flow — portable to cnos.

## Gaps

- cnos has no `release.sh` wrapper — release is still manual steps (7 commands). tsc wraps the same pattern into one command. Portable to cnos.
- tsc has no checksum generation in release workflow yet.
- Self-update checksum verification (#161) open.
