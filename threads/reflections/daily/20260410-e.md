# 2026-04-10-e — Design docs + OCaml skill + session wrap

## What shipped

- **OCaml skill v2** (`2940549`): full L6 rewrite — frontmatter, determinism, idempotence, cnos boundaries, kata. All 3 eng skills (Go, TypeScript, OCaml) now at same standard.
- **GO-KERNEL-COMMANDS.md** (`3b7e581`): design doc — the kernel is a package. One `Command` interface, one registry, three sources (kernel/repo-local/package-vendored). Prior art: OSGi, JPMS, Maven. cnos diverges by avoiding classloader hell, reflection magic, runtime resolution.
- **HYBRID-LLM-ROUTING.md** (`1cd2dd1`): design doc — routing policy in trusted core, LLM backends as capability providers. Local-default (≤8k tokens, ≤2 depth, ≤10 files), remote-default (>12k or architecture/release tasks), local-then-remote (escalate on validation failure). Every call leaves a receipt.
- **#207 filed**: Go purity boundary cleanup (F2–F4 from PR #206 review)
- **#209 filed**: Go kernel Phase 2 — CLI entrypoint + modular command dispatch

## Session totals

- 4 releases: v3.41.0 post-release captured, v3.42.0 (#180 + CDD patches), v3.43.0 (first Go code), OCaml skill v2
- 2 design docs committed
- 3 CDD process patches (hub memory Step 7, branch cleanup §2.6a, OCaml skill rewrite)
- 2 issues filed for Claude handoff (#207, #209)
- 13 merged branches cleaned

## MCI freeze status

Still active. 3 growing: #193 (7 cycles), #186, #175. Two new design docs committed — these are architecture-level, not new MCA commitments. The freeze allows clarification of existing designs; these qualify.

## Key decisions

- **The kernel is a package**: `cnos.kernel` has embedded `cn.package.json`, same Command interface as vendor/package commands. Adding a command = one file + one manifest entry regardless of source.
- **Hybrid LLM routing**: body-plane concern, not a skill. Routing matrix with hard thresholds, receipts for every call, tune from data not taste. Implementation deferred.
- **OCaml skill at L6**: same standard as Go/TypeScript. Three eng skills converged.
- **v3.43.0 = minor not major**: L7 significance but additive (Go alongside OCaml). 4.0.0 reserved for when Go replaces OCaml.

## Next

- #207 (purity cleanup) — Claude working on it
- #209 (CLI dispatch) — after #207
- Phase 3 (remaining kernel commands) — after #209
