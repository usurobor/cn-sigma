# Daily — 2026-04-07

## What happened

- Heartbeat: sync clean, inbox clear (all stale rejected messages from Feb)
- PR #166 (#155 remaining ACs) — CI green, merged, issue auto-closed
- Reviewed cnos state: 50 open issues, 2 open PRs, 8 unreleased commits on main since v3.33.0
- Loaded all CDD skills per gate

## Key work: #167 design

Axiom walked me through the real diagnosis behind #155. The conversation went:
- #155 → shallow-fetch failure → why can't Claude's git fetch by SHA? → git protocol limitation → but the real issue isn't git, it's using git as package transport → how should we distribute packages? → NuGet-style artifact delivery

This produced #167: package artifact distribution + commands as content class. L7 system-shaping move that:
- Eliminates the entire failure class #155 patched around
- Subsumes #162 (modular CLI commands)
- Simplifies lockfile from source/rev/subdir to name/version/sha256
- Adds commands as 6th explicit package content class

Design doc v0.2.0 iterated twice based on Axiom's review:
- v0.1.0: "commands inside packages" (too informal)
- v0.2.0: "commands as first-class content class" (matches package-system architecture)

## Decisions

- **Park v3.34.0 release** — #155 git fixes will be deleted by #167. Shipping them as a release then immediately removing them is churn. Ship #167 first, then release everything together.
- **Hand implementation to Claude Code** — wrote self-contained prompt with full spec reference to design doc on branch.

## Process observations

- Axiom's review method: push on the "why" until the root cause surfaces, then design from there. The first three answers (embed in binary, release assets, fetch by tag) were all treating symptoms. "The issue isn't in git — the issue is in how we distribute packages" was the real insight.
- CDD skill-loading gate worked correctly — loaded all skills before selecting next MCA.
- The "one hub exists, clean cut" simplification eliminated ~30% of the design complexity (migration phases, lockfile coexistence, backward compat).

## Afternoon: #167 implementation + CDD iteration

- Claude Code shipped PR #169 — all three stages (producer, HTTP restore, command discovery). CI green after two fixes (docstring placement, expect test).
- Review found gaps: 6 bare `with _ ->` catches (v3.32.0 regression), zero tests for cn_command.ml, zero tests for HTTP restore path, empty commands object.
- Root cause: handoff prompt was a feature spec, not a CDD contract. Missing: active skills, test requirements, engineering conventions, artifact order.
- **MCA: CDD §2.5a** — delegated implementation handoff protocol + self-verification gate. Added to both skill and canonical CDD.md.
- **MCA: CDD §4** — pipeline reference table added to canonical CDD.md §5.3 (step 6f: delegated handoff). Skill §4 now points to canonical tables instead of duplicating.
- **L7 diagnostic patterns** captured in architecture-evolution skill §5 (five patterns from #167 design session).
- **L8 candidate** tracked as #168 — method-shaping leverage, collecting evidence.
- **Package loading model** — surveyed Java/Ruby/npm/VS Code/NuGet. Chose NuGet: dumb package, smart framework. Design doc v0.3.0 specifies directory layout, loading conventions, skill orchestration stays in skills not manifest.

## Evening: ship + close

- Claude Code fixed F1-F3 (bare catches, cn_command tests, HTTP restore tests) in one commit
- Ran `cn build` locally to sync packages → I1 green
- All CI green → merged PR #169
- Released v3.34.0: CHANGELOG, RELEASE.md, tag, release workflow, package index updated
- Claude Code wrote post-release assessment (PR #171): C_Σ A- · α A- · β A- · γ B+ · L7
- Merged assessment → cycle closed

## Full session summary

One continuous session covering:
1. PR #166 merge (#155 completion)
2. #167 design (artifact-first distribution → NuGet model → commands content class)
3. #167 implementation handoff → Claude Code
4. Review findings → CDD §2.5a handoff protocol
5. Package loading model (NuGet: dumb package, smart framework)
6. Skill activation → runtime contract cognition layer
7. #170 spec (orchestrators as separate content class, not skills)
8. Fix findings → merge → release v3.34.0 → assessment → close

## What's next

- #170: orchestrators as mechanical workflow IR (design on main, needs implementation)
- #168: L8 candidate — collect more evidence
- Stale branch cleanup (~25 claude/* branches)
- Design doc cleanup: strip superseded activation sections from PACKAGE-ARTIFACTS.md (now #170 scope)
