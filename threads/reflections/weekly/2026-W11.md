# Weekly Review — 2026-W11 (Mar 9–15)

## Summary

Docs coherence week. Shipped v3.6.0 (output plane separation), then spent the bulk of the session restructuring the entire docs tree from Diátaxis → functional categories → triadic ontology (α/β/γ). Cleaned the repo root. Recovered Pi from an infinite retry loop.

## Highlights

- **cnos v3.6.0** released (Mar 12): cn_output.ml, output plane separation, projection.render.* trace events
  - Bug found during review: body consumption poisoned reply payloads via resolve_payload. Fixed with raw_coordination_ops
  - v3.5.2 content accidentally dropped in initial merge (branch forked from v3.5.1). Caught and restored
- **Pi recovery**: empty Telegram message caused infinite retry loop (Claude API 400). Advanced offset, cleared state. Binary updated manually
- **Binary update path** (#27): cn update no longer requires OCaml toolchain. Downloads pre-built binary from GitHub Releases. Merged and closed
- **Docs triadic restructure**: 5 restructure commits across ~200 files
  - α/ = pattern (what is articulated): 14 docs
  - β/ = relation (how parts cohere): 5 docs + guides/ + evidence/
  - γ/ = evolution (how the system moves): 4 docs + plans/ + checklists/
  - THESIS.md at docs root as "the whole, above the triad"
- **Key docs rewritten**:
  - FOUNDATIONS.md v2.0.0: "TSC defines coherence. cnos operationalizes it."
  - ARCHITECTURE.md v2.0.0: relation map, not protocol overview
  - WHITEPAPER.md v3.0.0: scoped to CN protocol, §10 replaced
  - COHERENCE-SYSTEM.md §3.4: CMP/MCP/CAP/CLP as instruction set
  - DOCUMENTATION-SYSTEM.md: taxonomy and placement rules
- **Repo root cleaned**: PLAN.md, RELEASE.md, ROADMAP.md removed. RULES.md → γ. spec/system/ removed. cn.json updated (kind: "system")
- **9 stale branches deleted**, 3 in-flight remain
- **CDD skill merged** (6 commits) + coherence-system branch (7 commits)
- **5 issues filed**: #27 (closed), #28, #29, #30, #34

## Key insight

α/β/γ are ontological dimensions, not measurement axes. The docs restructure was itself a live coherence loop (CMP → MCP → CAP → CLP), measured before/after: B+ → A.

CMP/MCP/CAP/CLP are four verbs (look/picture/move/check) — an instruction set, not vocabulary. This was captured in COHERENCE-SYSTEM.md §3.4 and issue #34 (runtime coherence loop).

## Carry-forward

- #28 daemon retry limit — Pi exposed this, still open
- #29 empty Telegram message handling — still open
- #30 temporal awareness — P1, agent has no concept of "now"
- #34 runtime coherence loop — v4.0-scale, CMP/MCP/CAP/CLP in trace events
- Pi runs as nohup, not systemd. No cron = no cn sync = stale runtime.md
- 3 in-flight branches: v3.7.0 scheduler, v3.8.0 syscall, v3.9.0 workspace
