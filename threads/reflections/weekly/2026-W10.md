# Weekly Review — 2026-W10 (Mar 2–8)

## Summary

Heavy engineering week. Brought cnos from v3.1 to v3.3.1 with two major releases and one patch. Onboarded Pi to native cnos runtime, discovered and fixed multiple operational issues. Identified structural gaps (skill provisioning, stale memory).

## Highlights

- **cnos v3.2.0** released (Mar 5): structured LLM schema — system blocks, multi-turn, cache control
- **cnos v3.3.0** released (Mar 7): CN Shell — typed ops, two-pass execution, path sandbox, crash recovery (25 commits, 33 files, ~5.6k lines)
- **cnos v3.3.1** patch (Mar 7): fixed Pi's XML hallucination caused by missing template/skills on Pi's machine
- **Pi onboarding** (Mar 3, 5): moved Pi from OpenClaw to cnos native runtime, systemd service, fixed model config, upgraded to Opus and v3.2→v3.3.1
- **Full context ingestion** (Mar 3, 5): read all cnos docs, skills, mindsets, specs, workspace memory
- **CN Shell spec** (Mar 5): designed two-pass execution, typed-op schema, auto-promote, path sandbox
- **CI fixes** (Mar 7): ppx_expect temp file race (TMPDIR isolation + -j 1)
- **Issue #22** opened: version string duplication across 3 files

## Patterns

- Strong review → fix → release → deploy pipeline emerged by end of week
- Two process violations early (committing/PR without approval) — corrected immediately, clean thereafter
- One destructive op mistake (`rm -rf ~/.openclaw` on Pi's server) — recovered from GitHub

## Carry-forward

- Skill provisioning gap: no mechanism for new agents to receive skills/mindsets
- Memory files stale since Feb 11 — need update
- W09 weekly review never filled (quiet week?)
- Issue #22 (version string duplication) open
