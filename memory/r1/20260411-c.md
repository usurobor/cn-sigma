# 2026-04-11-c — Repository restructure: src/packages, activation table, YAGNI cleanup

## What happened

Major repo restructure in one session. ~15 commits, net thousands of lines deleted.

### Layout changes
- `go/` → `src/go/` — all source under `src/`
- `src/agent/` → `src/packages/{cnos.core,cnos.cdd,cnos.eng}` — modular package layout per BUILD-AND-DIST.md
- `src/cli,cmd,lib,ffi,transport,protocol` → `src/ocaml/` — ceased OCaml grouped
- `packages/` deleted — was legacy intermediate build output
- `profiles/` deleted — both identical, unused by code
- `spec/` deleted — stale templates, canonical versions in src/packages/cnos.core/templates/
- `cnos.agent`, `cnos.hub`, `cnos.net.http` placeholders deleted — YAGNI until Phase 5
- Go module path: `cnos/go` → `cnos/src/go`, all imports + CI updated
- `FindIndexPath` → looks for `dist/packages/index.json`

### Package manifests
- Each package has `cn.package.json` with independent versioning (not locked to binary)
- `skills.exposed` / `skills.internal` declared per PACKAGE-ARTIFACTS.md design
- cnos.cdd: 1 exposed (cdd orchestrator), 6 internal sub-skills with `parent: cdd`
- cnos.core: 21 exposed, 3 commands
- cnos.eng: 19 exposed

### Skill activation
- All 47 skills now have `triggers:` in frontmatter
- CDD sub-skills declare `parent: cdd`
- Runtime can derive activation table mechanically from installed packages

### Docs updated
- README rewritten: vision → honest current state → roadmap to v4
- PACKAGE-SYSTEM.md v2.0.0: all paths updated for co-located layout, `sources` field removed
- INVARIANTS.md T-003: OCaml ceased (previous commit)
- Hub reflection for OCaml cessation milestone (20260411-b)

## Design alignment

The repo now matches the design docs:
- BUILD-AND-DIST.md: `src/packages/` → `cn build` → `dist/packages/` → `cn deps restore` → `.cn/vendor/packages/`
- PACKAGE-ARTIFACTS.md: exposed/internal skills, triggers, activation table
- PACKAGE-SYSTEM.md: 7 content classes, co-located with manifests

## What's next

- Phase 4: package command discovery + dispatch in Go
- `cn build` needs updating to read from `src/packages/` (currently still has old paths in Go code)
- Activation table implementation in Go runtime (Phase 5)
- Pi stays stopped until Go runtime replaces OCaml

## State
- Main: `ebd7ef8`
- All 63 Go tests pass
- Pi service: stopped and disabled
