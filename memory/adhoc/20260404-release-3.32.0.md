---
title: Release 3.32.0 — legacy fallback audit + CDD normalization
date: 2026-04-04
status: closed
relates_to: ["#152", "#153", "#156"]
---

## What happened

Released 3.32.0 with three main components:
1. Legacy fallback audit (#152): 29 findings, 7 removed, 15 converted, 7 kept
2. CDD bundle normalization: all 7 skills have full frontmatter + kata
3. Design docs: thread event model (#153), hub placement models (#156)

## PR #157 review history

5 rounds, 10 findings:
- R1 (operator): audit incomplete on cn_assets.ml, artifact parity on validate_path, unicode warnings, CI
- R2 (sigma): scope narrowing unacknowledged (13 `with _ ->` in untouched files), CI red, cn_deps.ml 2 remaining
- R3 (operator): PR body stale (17 vs 25), CI not green
- R4 (sigma): CI red — package/source drift from main (our CDD normalization commits)
- R5 (sigma): approved, all green

## Package/source drift incident

CDD normalization commits (`6f00c66`, `2a64a8a`) updated `src/agent/skills/cdd/` but didn't sync to `packages/cnos.core/skills/cdd/`. This caused I1 CI failure on any branch rebased onto main. Fixed with `8b2fd17`.

Also accidentally leaked PR #157 branch artifacts (`docs/gamma/cdd/3.32.0/README.md`, `SELF-COHERENCE.md`) to main during a `git checkout` for diff comparison. Cleaned up in `fe83432` + `31359f8`.

**Lesson:** After changing src/agent/skills/, always sync packages/ (via `cn build` or manual copy).

## Skill loading gate — third violation

Started release without loading `cdd/SKILL.md`. Loaded `cdd/release/SKILL.md` only. Output was correct but process had gap. Same root cause as 3.31.0: urgency bias. No MCA exists at agent layer.

## Post-release assessment

- α A, β A, γ A- (revised from provisional B+)
- 5 review rounds (improved from 13 in 3.31.0)
- Next MCA: #153 (thread event model) — deferred to fresh session
