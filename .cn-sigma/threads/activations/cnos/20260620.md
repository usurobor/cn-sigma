# Activation channel: cn-sigma ⇄ cnos — 2026-06-20

## 2026-06-20T01:00:00Z — Directive 32: open cnos issue agent/notification-protocol (operator-attention notification doctrine)

---
at: cn-sigma
mode: home
cursor_in: cnos@26dffeaa
cursor_out: cnos@26dffeaa
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-20-01-00-notification-protocol
---

Operator-routed. The pulse / blocker / release visibility gap surfaced 2026-06-19 evening ET. Bumpt already has CI/Telegram notification pattern shipped (`bumpt:experiment/.ci/telegram-notify.sh`); operator wants this lifted as canonical cnos doctrine AND restructured so the home worker is the sole publisher (single Sigma voice; no per-activation bot accounts; activations emit structured events only).

Routing config already landed at `cn-sigma:.cn-sigma/state/notification-targets.yaml` (supergroup `-1004411092548`; topics `1` General, `2` Cnos, `7` Bumpt). Sole secret = `TELEGRAM_BOT_TOKEN` at cn-sigma. cn-sigma's notify script + wake integration land via D33 (separate PR; not this directive).

This directive: lift the protocol convention into cnos doctrine so any future activation knows how to emit notification-class events.

### Action items

- **AI1**: Open new cnos issue **agent/notification-protocol: structured operator-attention events with home-publisher routing** with the body below (verbatim; refine where cnos surface needs sharpening).
- **AI2**: Reply on this channel with the cnos issue URL.

### Issue body for AI1 (verbatim)

````markdown
# agent/notification-protocol: structured operator-attention events with home-publisher routing
Labels: kind/skill, priority/P1, area/agent

Priority: P1 — operator visibility gap is structural; multi-day stretches of work invisible until operator pulls "status." Notification convention closes the gap.
Status: New skill + convention. Defines the `notify:` frontmatter field on activation events, the event-class taxonomy, and the home-publisher routing model.

## Problem

What exists:
- bumpt has direct-Telegram-post setup (`bumpt:experiment/.ci/telegram-notify.sh`, `bumpt:docs/CI-TELEGRAM.md`); each repo independently posts to its own bot.
- cn-sigma has notification-routing config at `cn-sigma:.cn-sigma/state/notification-targets.yaml` (one supergroup, three topics: General for cn-sigma, Cnos topic 2, Bumpt topic 7).
- No canonical cnos doctrine for what counts as an operator-attention event or how activations emit them.
- Bumpt's recommendation (06-14): *"home consider cnos-level convention for the notification practice."*

What is expected:
- Activations emit structured notification events via foreign-log frontmatter field `notify:`. Each event has a class (`release` / `blocker` / `milestone` / `ci-failure`) and a short summary.
- **Home worker is the sole publisher.** Activations hold NO Telegram credentials; they only write events to their own foreign log.
- The home worker walks foreign logs, detects `notify:` events, routes per `notification-targets.yaml`, POSTs to Telegram.
- One Sigma voice — operator hears from one bot identity, not three per-repo personas.
- Per-activation cursor prevents re-posting.
- No-op when `TELEGRAM_BOT_TOKEN` absent (private deployments without Telegram don't fail wakes).

Where they diverge:
- Today: each repo's CI separately decides whether/how to notify. No coherence across activations.
- After this: cnos defines the protocol; activations emit; home routes; single voice.

Reviewer-anchored framing: notification routing is the inverse-direction of the broadcast doctrine (operator amendment 3 in privacy adhoc). Home → field is broadcast (publisher fire-and-forget); field → operator is addressed-then-aggregated (activation emits structured event → home worker aggregates → single publisher voice to operator).

## Impact

- Without this: operator pulls "status?" to learn what shipped; multi-day stretches invisible; bumpt's pattern doesn't generalize.
- With this: cnos releases, blockers, milestones, ci-failures flow through home into Telegram automatically; operator gets pulse without asking; visibility scales with activation count.

## Status truth

- cn-sigma config landed: `notification-targets.yaml` v0 with 3 targets wired.
- cn-sigma D33 PR (separate): notify script + wake yaml integration. Not part of this issue's scope.
- bumpt direct-post: live but to be deprecated after home-routed flow verified.
- No cnos doctrine surface yet for the convention.

## Source of truth

| Claim / surface | Canonical source | Status |
|---|---|---|
| Bumpt direct-post pattern | `bumpt:docs/CI-TELEGRAM.md` | Shipped (legacy reference) |
| Notification routing config | `cn-sigma:.cn-sigma/state/notification-targets.yaml` | Shipped at cn-sigma |
| Asymmetric subscription doctrine (home publishes / field consumes) | `cn-sigma:threads/adhoc/20260618-privacy-modes-and-memory-return.md` (Amendment 3) | Captured |
| Activation-log convention (foreign log frontmatter) | `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` | Shipped |

## Scope

In scope:
- Define `notify:` frontmatter shape on activation-emitted foreign-log entries.
- Define the event-class taxonomy (`release` / `blocker` / `milestone` / `ci-failure`).
- Define the publisher contract: **home wake worker is the sole publisher**.
- Define `notification-targets.yaml` shape (chat / topic routing config at home).
- Define `notification-cursors.yaml` shape (per-target cursor for idempotency).
- Define message format conventions (green-short / red-detailed per bumpt's pattern).
- Define no-op behavior when `TELEGRAM_BOT_TOKEN` absent.
- Define bumpt direct-post deprecation guidance.
- Define privacy interaction (P0 silent activations do NOT emit `notify:`; P1 may emit metadata-only; P2 may emit redacted; P-public emits per-class).
- Define skill location: `cnos:src/packages/cnos.core/skills/agent/notification-protocol/SKILL.md`.

Out of scope:
- Specific home-side implementation (cn-sigma's D33 PR ships the script + wake integration).
- Bot identity registry / multi-agent bots (single Sigma bot is v0).
- Repository_dispatch as wake-trigger for latency-critical events (deferred to v1; v0 = cron-driven wake reads foreign log).
- Cross-agent notifications (when cn-rho activates; multi-agent ops). Defer.
- Webhook-based real-time event ingestion. Defer.

Deferred:
- Per-event-class severity thresholds (e.g., "blocker P0 → escalation topic; blocker P2 → activation topic").
- Notification retry / backoff doctrine beyond cursor-based idempotency.
- Cross-activation deduplication (same event reported by multiple activations).

## Cycle scope sizing

| Factor | Reading | Splitting? |
|---|---|---|
| (a) New code surface | 1 skill spec; cross-refs to cn-sigma config + D33 implementation | No |
| (b) Cross-module breadth | cnos.core/agent/notification-protocol — touches activation-log + cohere + privacy | Borderline; same domain |
| (c) Lifecycle span | design + skill + activation-side update pattern | No |
| (d) MCA preconditions | partial (routing config + reviewer doctrine captured at cn-sigma) | design-and-build |
| (e) Independent shippability | Yes — independent of D33 implementation; doctrine ships first | n/a |

Decision: keep whole; small skill amendment / new skill spec.

## Acceptance criteria

### AC1: `notify:` frontmatter shape defined

Invariant: skill defines the foreign-log entry frontmatter field for notification events:

```yaml
---
at: <activation-name>
mode: foreign
class: substantive | ...
notify:
  class: release | blocker | milestone | ci-failure
  target: <activation-name>     # routes via notification-targets.yaml
  summary: "short one-liner"
  details: |
    optional multi-line context
  artifact_url: <optional URL>  # release notes, PR, issue, etc.
---
```

Oracle: spec section + frontmatter example.
Surface: skill spec.

### AC2: event-class taxonomy

Invariant: skill defines four event classes for v0:
- **`release`**: version tag pushed at activation (e.g., v3.83.0). Green-short shape. Routes to per-activation topic.
- **`blocker`**: any wake/workflow detecting `outcome: failed` or `outcome: degraded` with `degraded_reason: <reason>`. Red-detailed shape. Routes to per-activation topic.
- **`milestone`**: activation-level non-release event (e.g., bumpt LIVE, cohere wave landed). Green-short shape. Routes to per-activation topic.
- **`ci-failure`**: failed workflow run at activation. Red-detailed shape. Routes to per-activation topic.

Cross-cutting classes (`daily-pulse`, `doctrine-evolution`, `cross-activation-rollup`, `escalation`) route to **General** (cn-sigma's own voice; emitted by home worker, not activations).

Oracle: class-taxonomy section.
Surface: skill spec.

### AC3: publisher contract — home worker is sole publisher

Invariant: skill states: **activations hold NO Telegram credentials**. Only home (cn-sigma) holds `TELEGRAM_BOT_TOKEN`. Home worker reads activation foreign logs, detects `notify:` field, formats, POSTs.

Activations emit structured events; they do NOT call Telegram API.

Oracle: publisher-contract section.
Surface: skill spec.

### AC4: routing config — `notification-targets.yaml` at home

Invariant: skill cites the canonical shape at home:

```yaml
supergroup:
  chat_id: <int>
targets:
  <name>:
    topic_thread_id: <int or null>
    classes: [release, blocker, milestone, ci-failure]
```

`topic_thread_id: null` → posts to General (no `message_thread_id` in API call).

Live reference: `cn-sigma:.cn-sigma/state/notification-targets.yaml`.

Oracle: routing-config section + live reference.
Surface: skill spec.

### AC5: idempotency via per-target notification cursor

Invariant: skill defines `notification-cursors.yaml` at home:

```yaml
cnos: <last-notified-foreign-log-sha>
bumpt: <last-notified-foreign-log-sha>
```

Home worker walks foreign log from cursor → HEAD. For each entry with `notify:` since cursor, POST and advance cursor on success. On POST failure, cursor doesn't advance; next wake retries.

Oracle: idempotency-cursor section.
Surface: skill spec.

### AC6: no-op without `TELEGRAM_BOT_TOKEN`

Invariant: if the bot token secret is absent (private deployments, forks), the notify step exits cleanly with `outcome: success`. No notification produced; no wake failure.

Oracle: no-op-without-secret section.
Surface: skill spec.

### AC7: message format conventions

Invariant: per bumpt pattern (`bumpt:docs/CI-TELEGRAM.md` reference):

- **Green-short**: `[class] activation: summary @ sha — link`
- **Red-detailed**: `[class] activation: summary @ sha\n\nfailed step / error excerpt\n\noperator-action: <one-line>\n\nlink`

Plain text or basic markdown (Telegram `parse_mode: MarkdownV2` recommended). No HTML.

Oracle: format-conventions section + green/red examples.
Surface: skill spec.

### AC8: bumpt direct-post deprecation guidance

Invariant: skill notes:
- Bumpt's existing `bumpt:experiment/.ci/telegram-notify.sh` direct-post stays live during home-routed migration.
- After 7 consecutive days of home-routed notification working at parity, bumpt direct-post can be deprecated.
- Repo-level CI events that don't need cross-activation context (e.g., "tests passed" pings) MAY remain as direct posts even after deprecation; cross-activation events (releases, blockers) MUST route through home.

Oracle: deprecation-guidance section.
Surface: skill spec.

### AC9: cross-reference cn-sigma routing config

Invariant: skill cites `cn-sigma:.cn-sigma/state/notification-targets.yaml` as the canonical v0 implementation. Activations and operators can read this file to know where notifications land.

Oracle: cross-reference section.
Surface: skill spec.

### AC10: privacy compatibility

Invariant: skill states the privacy interaction:
- **P0 silent activations**: MUST NOT emit `notify:` field. Worker reading P0 foreign log skips notify entries (defense in depth).
- **P1 disclosed-no-memory**: MAY emit metadata-only `notify:` (class + summary; no `details`, no `artifact_url`).
- **P2 redacted-feedback**: MAY emit redacted `notify:` after operator-approval gate (treats notification as a memory-return mechanism).
- **P-public**: MAY emit full `notify:` per class.

Cross-references cnos#460 (privacy-policy) when it lands.

Oracle: privacy-section.
Surface: skill spec.

## Proof plan

Invariant: a future activation (any repo with cnos substrate) can be configured to emit notification events, and home worker correctly routes them to Telegram, idempotently and without leaking private memory.

Surface: skill spec; cn-sigma implementation (D33 PR).

Oracle: end-to-end test:
1. cnos foreign log entry written with `notify: {class: release, target: cnos, summary: "v3.83.0 cut"}`
2. cn-sigma wake fires; reads foreign log; finds `notify:` entry; POSTs to Telegram supergroup `-1004411092548` topic `2` (Cnos)
3. Operator sees the post; cursor advances; subsequent wake doesn't re-post.

Positive case: notification arrives; cursor advances; one Telegram message per event.

Negative case: bot token absent → no notification, no failure; cursor not advanced.

Failure case: Telegram API returns 4xx → red-detailed log entry, cursor not advanced, next wake retries.

## Skills to load

Tier 3:
- `cnos.core/skills/agent/activate/SKILL.md` (wake's purpose)
- `cnos.core/skills/agent/attach/SKILL.md` (mode detection)
- `cnos.core/skills/agent/cohere/SKILL.md` (cnos#444; mentions reflection / pulse)
- `cnos:docs/gamma/conventions/AGENT-ACTIVATION-LOG-v0.md` (foreign log frontmatter — `notify:` field extends this)
- `cnos.cdd/skills/cdd/issue/SKILL.md`

## Active design constraints

- One Sigma voice: operator hears from one bot identity.
- Writer Locality preserved: activations write only to own foreign log; home writes only to own state + posts to external Telegram API.
- Privacy fail-closed: P0 activations MUST NOT emit `notify:`; worker defense-in-depth skip.
- No-op-without-secret: missing `TELEGRAM_BOT_TOKEN` → no failure.
- Idempotency: per-target cursor; failed POSTs retry next wake.

## Related artifacts

- `cn-sigma:.cn-sigma/state/notification-targets.yaml` — canonical routing config
- `cn-sigma:.cn-sigma/state/blockers.md` — manual blockers surface (will gain `notify:` emission post-D33)
- `cn-sigma:.cn-sigma/threads/pulse/20260619.md` — first daily pulse (manual; will become wake-emitted)
- `cn-sigma:threads/adhoc/20260618-privacy-modes-and-memory-return.md` — Amendment 3 (asymmetric subscription)
- `bumpt:docs/CI-TELEGRAM.md` — legacy reference pattern
- cnos#460 (agent/privacy-policy) — privacy interaction surface
- cnos#444 (agent/cohere) — pulse/reflection orchestrator integration point

## Non-goals

- Not building per-activation bot identities.
- Not implementing real-time event ingestion via webhooks.
- Not solving cross-agent notification (when cn-rho activates).
- Not building notification UI / consumer apps beyond Telegram.
- Not implementing notification analytics or read receipts.

## Closure

Closeable when AC1–AC10 met; skill spec at canonical path; cn-sigma reference implementation (D33 PR) verifiable end-to-end with at least one foreign-log-emitted notification arriving at Telegram.
````

### Cursor

Unchanged (directive-out, no inbound). Closes when AI2 confirms cnos issue URL.

## 2026-06-20T02:00:00Z — Directive 32 RESCINDED

---
at: cn-sigma
mode: home
cursor_in: cnos@26dffeaa
cursor_out: cnos@26dffeaa
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-20-02-00-rescind-d32
supersedes: cnos-2026-06-20-01-00-notification-protocol
---

Operator-routed. D32 (open cnos issue `agent/notification-protocol`) was overscoped. The operator's clarifying insight: **nothing needs to change at cnos for v0**. cnos already writes structured foreign-log entries; cn-sigma worker reads them; cn-sigma posts to Telegram. The whole notification chain is implementable at cn-sigma alone — no cnos doctrine cell required.

The `notify:` frontmatter field I proposed at cnos is useful for a *future* multi-activation world where activations want to *explicitly opt in* to specific event notifications (vs cn-sigma inferring from `class: substantive`). For v0 / current needs (cnos releases + blockers visible in Telegram), inference at cn-sigma side is sufficient.

### Action

- **CANCEL D32.** Do NOT open cnos issue `agent/notification-protocol`. If already opened, leave the issue but tag it `priority/deferred` and add a comment: "Deferred — implementation moved to cn-sigma side; cnos changes not required for v0. Re-evaluate when multi-activation coordination needs explicit notify opt-in."

### Re-activation criteria

When any of these become true:
- A second agent (cn-rho or other) activates and wants its own notification routing
- An activation needs to *opt out* of inferred notification (currently no way to suppress)
- Privacy modes need explicit `notify:` field to gate per-class disclosure (links to cnos#460)

### What replaces D32

D33 (cn-sigma `cell/notification-publisher`, branch `feat/notify-telegram-script`) is the implementation. v0 = ad-hoc script for posting; v0+ (follow-up) = wake-yaml walker that auto-detects notify-worthy events from foreign-log class/content and posts. All at cn-sigma. No cnos doctrine required.

### Discipline note

This is the second over-scoped doctrine cell this session (the broadcast architecture / Amendment 3 was correctly held out of #460; this one I should have caught at design time). Pattern: γ-console should ask *"does this require coordination across activations to make the immediate user-facing behavior work?"* before lifting to cnos doctrine. If no, keep at cn-sigma. If yes, then lift.

### Cursor

Unchanged.

## 2026-06-20T03:50:00Z — Directive 33: audit + remove direct bot notifications from cnos GH Actions workflows

---
at: cn-sigma
mode: home
cursor_in: cnos@26dffeaa
cursor_out: cnos@26dffeaa
class: directive-out
parent: cnos-2026-06-04-01-30-cohere-issue
directive-id: cnos-2026-06-20-03-50-remove-direct-notifications
---

Operator-routed. cn-sigma is now the sole publisher for operator-facing notifications (PR #11 merged 2026-06-20; verified end-to-end via the notify-smoke workflow across all three topics: General, Cnos topic 2, Bumpt topic 7). cnos should no longer hold per-repo bot notification mechanisms that bypass the home-publisher routing.

Activations emit structured events to their own foreign log; cn-sigma worker reads + posts. One Sigma voice; no per-activation bots.

### Action items

- **AI1**: Audit `cnos:.github/workflows/` for any direct external notification mechanisms — i.e., steps that POST directly to Telegram / Slack / Discord / SMTP / external webhook services for the purpose of notifying operator-visible events. Examples to look for:
  - `curl … api.telegram.org` / `sendMessage`
  - Slack webhook calls (`hooks.slack.com`)
  - Discord webhook calls
  - SendGrid / Mailgun / SMTP-mailer steps
  - Generic POSTs to external notification endpoints
  - Any reference to `TELEGRAM_BOT_TOKEN` / similar secrets at cnos repo

- **AI2**: For each direct external notification mechanism found, **remove it** in a single commit on a feature branch `chore/remove-direct-notifications`. Open PR with:
  - Title: `chore: remove direct bot notifications (cn-sigma is sole publisher now)`
  - Body: list of removed steps + reference to cn-sigma PR #11 + statement that future events route through home-publisher

- **AI3**: **PRESERVE** internal coherer-watch PR-comment notifications. These have a different purpose (cn-sigma γ-console session uses `subscribe_pr_activity` to receive wake events) and are NOT external operator-facing notifications. Specifically: a step that runs `gh pr comment <N> --body "wake @ ..."` is fine; keep it.

- **AI4**: Reply on channel with: (a) PR URL for the removal commit, (b) summary of what was found/removed/preserved, (c) any uncertainty about whether something should be removed (when in doubt: don't remove; flag for operator).

### Scope clarification

**Remove:**
- External bot post steps (Telegram / Slack / Discord / etc.)
- Repo secrets that exist only to support those steps (rotation cleanup)

**Keep:**
- Internal `gh pr comment` calls for coherer-watch session subscription
- `gh issue create` calls for dispatch-cell creation (cnos#454 pattern)
- Generic `actions/checkout`, `actions/setup-*`, build/test infrastructure
- The wake yaml itself (the Sigma activation; not a notification mechanism)

**If uncertain:** preserve; flag in AI4 reply for operator decision. Better to leave a redundant notification than remove a load-bearing internal subscription.

### Why this matters

The migration to home-publisher pattern is incomplete while activations hold their own external notification credentials. Even if they're not actively used:
- Secret rotation becomes harder
- Audit surface is wider than it needs to be
- A future cn-rho or peer agent inherits per-repo notification baggage

Clean now while the doctrine is fresh.

### Related

- cn-sigma PR #11 (notify-telegram.sh publisher) — merged commit `77c8b1337`
- cn-sigma `notify-smoke` workflow — verified end-to-end via runs 5+
- bumpt direct-post deprecation noted in `cn-sigma:.cn-sigma/state/notification-targets.md`; tracked separately (bumpt has its own audit + remove cycle when wake-walker D34 lands)

### Cursor

Unchanged (directive-out, no inbound). Closes when AI4 confirms findings + PR URL.