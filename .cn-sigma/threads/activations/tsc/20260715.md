## 2026-07-15T21:40:09Z — activate+attach: tsc advances d1755173→26aab502 (first walk since inaugural; 3 substantive entries, 9-day backlog cleared)

---
at: cn-sigma
mode: home
cursor_in: tsc@d17551732937df0b2478e2e1a0a900f11d09ab72
cursor_out: tsc@26aab5023f03dc7d0abf82e5fdba20134fc6adad
class: substantive
---

Operator-triggered activate+attach run ("Execute the activate + attach skills (cnos.core/skills/agent/{activate,attach}/SKILL.md) for hub https://github.com/usurobor/cn-sigma"). Read `.cn-sigma/state/activations.md`; registered activations cnos, bumpt, tsc — tsc registered 2026-07-06 (see `.cn-sigma/threads/activations/tsc/20260706.md`, this thread's inaugural entry). Every routine walk since inaugural registration excluded tsc, citing "the operator's explicit list" — traced to a literal reading of this recurring prompt's own `(cnos, bumpt)` parenthetical, not a distinct operator ruling (see the sibling cnos-activation entry, same timestamp, for the full trace). tsc is walked this wake for the first time since inaugural.

Cloned `usurobor/tsc` fresh; `origin/main` HEAD = `26aab5023f03dc7d0abf82e5fdba20134fc6adad`, three commits ahead of the recorded cursor `d17551732937df0b2478e2e1a0a900f11d09ab72` (`git log d1755173..26aab502 -- .cn-sigma/logs/` → 3 commits, all appended to `.cn-sigma/logs/20260706.md`; no commits since — tsc's own foreign log has been static for 9 days).

**`5f73ce3b3` (2026-07-06T10:30:32Z) — "sigma: channel directive-out — resolve FSM-cn release blocker via cnos-sigma":** Operator directed Sigma-at-tsc (early adopter of the CDS FSM) to resolve the blocker to shipping the FSM toolchain to tenants, via cnos-sigma, then install `cn` at tsc. Investigated: `cn issues fsm evaluate/scan`, `cn cell finalize`, `cn install`, `cn install-wake`, and the `cds-dispatch` orchestrator existed only on cnos `main`, not in the latest released `cn` (3.82.0, 2026-05-24) — no FSM-capable binary was installable by a tenant. Compounded by D14 (holding the 3.83.0 version cut) and a tag-push auth bug in `release.yml`. Resolution path: publish a 3.82.1 patch release from `main` without disturbing the D14-held 3.83.0 cut, operator-authorized for this one-off.

**`8f596567` (2026-07-06T10:41:53Z) — "sigma: channel — FSM cn shipped (3.82.1) + installed at tsc (early-adopter success)":** Blocker resolved as cnos-sigma — published cnos 3.82.1 (FSM-capable `cn-linux-x64` + refreshed `cnos.core`/`cnos.cds` tarballs + new `cnos.issues` package) via a one-off self-deleting workflow off `main` (cnos@d8d1c6a); R1 failed on a gate-name typo, R2 green. Installed `cn` at tsc via `install.sh`, checksum-verified; `cn issues fsm` and `cn cell finalize/return/resume` confirmed present. Early-adopter install: success. Named as remaining: tenant `.cn/` setup, FSM label set, and rendering the agent-admin/cds-dispatch wakes via `cn install-wake` — flagged that live dispatch would need a `SIGMA_WORKFLOW_PAT` secret at tsc (the FSM engine itself does not).

**`26aab502` (2026-07-06T10:48:29Z) — "sigma: channel — customer challenges captured at cnos#606 (P1)":** Operator directed that every gap encountered be reported to home as a real customer challenge, filed as a P1 cnos issue. Filed **cnos#606** (P1): C1 no FSM-capable `cn` for tenants (resolved via 3.82.1), C2 fragile/version-coupled release path, C3 CLI ergonomics (no `--help` convention, stray-hub scaffold), C4 `cn init` scaffolds a full agent-hub with no lightweight CDS-FSM-tenant footprint, C5 wakes are cnos-self (build from `src/go`, no shipped tenant wake render), C6 hard `SIGMA_WORKFLOW_PAT` dependency with no `GITHUB_TOKEN` fallback for the PAT-free FSM subset, C7 no standalone label-sync for a hub-less tenant. Flagged C5+C6 as the load-bearing blockers to tenants actually running the wakes.

**Live-status check on cnos#606 (via `gh api`, this wake):** still `OPEN`, label `P1`, 2 comments, `updatedAt: 2026-07-06T19:41:55Z` — both comments are the operator's, same day as filing. C1–C7 were triaged into wave **cnos#607** (installer wave; subs #608 `install.sh` fail-closed guard, #609/#610 tenant-portable wake acquisition — addresses C5, #612 CLI ergonomics — C3, #613 PAT-free FSM engine + hub-less labels — C6/C7, #608 lightweight scaffold — C4). C1/C2 (release-channel fragility) were split out as a dedicated P1 release-infra cell, **cnos#618**, kept separate from the installer wave per operator ruling — #608 fail-closes on a pre-FSM `cn` in the meantime. The disposition (comment 2) is the last activity on #606; whether #607/#618's subs have since landed is cnos-side wave-tracking, out of scope for this tsc-foreign-log walk — flagging as a thread to pick up on the next cnos-activation walk (`.cn-sigma/threads/activations/cnos/`) rather than duplicating wave-status tracking here.

**Assessment.** All three entries are already-resolved-same-day narrative from Sigma-at-tsc (the FSM blocker was fixed and the customer-challenge issue was filed within 20 minutes of each other on 2026-07-06); none carry an open ask back to home beyond the record itself. tsc's own `.cn-sigma/logs/` has had zero commits in the 9 days since — no evidence tsc-agent-admin or a cds-dispatch wake has run there since, consistent with C5/C6 (wake portability, PAT dependency) still being unresolved blockers to tsc standing up its own live wakes. No home→tsc directive to send this wake; nothing to defer.

**Confirm:** Σ at cn-sigma (home) | operator: usurobor | hub: github.com/usurobor/cn-sigma | reading tsc activation. Three new foreign-log entries this wake (all from Sigma-at-tsc's 2026-07-06 session); cursor advances to `26aab5023f03dc7d0abf82e5fdba20134fc6adad`; no drift, no open action for home beyond recording. Sibling cnos and bumpt activations both advanced by one routine heartbeat each this same wake — see their respective threads.
