# 2026-04-11-b — OCaml ceased: Go is sole implementation language

## What happened

- Commit `aa7f6ee`: all CI, coherence, and release workflows switched from OCaml to Go
- `ci.yml` — Go build/test/vet/dispatch-boundary/smoke replaces OCaml job; `go.yml` deleted (merged in)
- `coherence.yml` — I1 uses `go run ./cmd/cn build --check`; OCaml test runner dropped from I2/I3
- `release.yml` — 4-platform Go binary builds replace OCaml
- INVARIANTS.md T-003 updated: "Go is the sole implementation language… OCaml is ceased"
- OCaml source stays in-tree as dead code until Phase 5 deletes it
- Pi stays on v3.50.0 OCaml binary until Go runtime replaces it

## Why this matters

- Removes the "no one can install OCaml" blocker for new users
- New users get a working Go binary from next release onward
- Marks the boundary between Phase 3 (kernel commands) and Phase 4 (package discovery)
- OCaml served cnos from inception through v3.50.0 — this is the end of that era

## Phase status

| Phase | Status |
|-------|--------|
| 1 — CLI skeleton | ✅ complete |
| 2 — core commands (help, init, status, doctor) | ✅ complete |
| 3 — build commands (deps, build, setup, update) | ✅ complete (v3.50.0) |
| 4 — package command discovery | next |
| 5 — agent runtime → 4.0.0 | future |

## State

- Main: `aa7f6ee` (head)
- VPS: v3.50.0 deployed, Pi service stopped (disabled pending Go runtime)
- MCI freeze: active, 6 growing items
