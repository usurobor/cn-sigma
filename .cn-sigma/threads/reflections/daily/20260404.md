---
date: 2026-04-04
---

## Summary

Released 3.32.0: legacy fallback audit (#152), CDD bundle normalization, design docs for thread event model (#153) and hub placement models (#156).

## Key events

- PR #157 reviewed across 5 rounds (R1-R5), 10 findings total
  - R1 (operator): audit incomplete, artifact parity, unicode, CI — 4 findings
  - R2 (sigma): scope narrowing, CI, cn_deps.ml gaps — 3 findings
  - R3 (operator): PR body stale, CI — 2 findings
  - R4 (sigma): CI red (package/source drift from main) — 1 finding
  - R5 (sigma): approved, all green
- Package/source drift discovered on main — CDD normalization commits updated src/ but not packages/. Fixed.
- Accidentally leaked PR branch artifacts to main during checkout; cleaned up in 2 commits.
- 3.32.0 released, deployed to Pi, validated.
- Post-release assessment: α A, β A, γ A- (revised from provisional B+).
- faster-whisper installed for voice note transcription (base + small + medium models tested). Works on CPU but accuracy limited on short clips.
- Hub placement models design doc + implementation plan committed and linked from #156.
- CDD bundle final normalization: all 7 skills have full frontmatter, governing questions, kata.

## Process observations

- Skill loading gate violated again (release started without loading cdd/SKILL.md). Same root cause as 3.31.0: urgency bias. No MCA at agent layer. Output was correct but process had gap.
- Package/source drift is a real hazard — updating src/agent/skills/ without syncing packages/ will break I1 CI on any branch that rebases. Need to remember `cn build` (or manual copy) after skill file changes.

## Decisions

- Next MCA: #153 (thread event model) — deferred to fresh session with full context.
- Gamma revised B+ → A- based on post-release assessment evidence.

## What went well

- Review quality: 5 rounds vs 13 for 3.31.0, cleaner convergence
- Operator + sigma review complementary — operator caught audit incompleteness, sigma caught scope narrowing and cn_deps.ml gaps
- Release notes follow CDD format fully

## What to watch

- Package/source sync after skill file changes
- Skill loading gate — still no MCA, still recurring
