# 2026-04-28 — Daily Reflection

## Done

- **Issue #283 filed** — Replace GitHub PR workflow with `.cdd/unreleased/{N}/` artifact exchange in CDD. Substantial cycle, 10 files. Three path corrections during drafting (`.cn/` → `.cdd/`, role-first → issue-first, dropped numbered prefixes). γ dispatched.
- **Issue #284 filed** — SOUL as CTB skill signature. Mapped every CTB LANGUAGE-SPEC concept to its SOUL prose equivalent. Two-table contrast (where we are / where we want to be). Operator chose triadic cycle over small-change to test the triad.
- **CTB ↔ SOUL connection articulated** — The SOUL is a CTB module written in prose. Key insight: SOUL is the activation context (CTB §3 scope model), its invariants are global aspects (§7), its continuity rule is the authority hierarchy (§8). Not "like" a module — IS one.

## Observations

- Issue drafting required three corrections on #283 before γ accepted it. The issue skill's quality gate (§2.4) would have caught the path error if I'd verified against the actual repo state before writing. UIE gap: I skipped Understand (check `.cdd/` layout) and went straight to Execute (write the issue).
- Operator is using #284 as a triad test — the content is medium complexity but the process test is the real goal.
- Connection between #277 (SOUL content rewrite) and #284 (SOUL structural bridge to CTB) may converge into one cycle.

## Decisions

- Both #283 and #284 go through CDD triadic cycles, not small-change
- `.cdd/unreleased/{N}/` is flat: one dir per issue, role in filename, no numeric prefixes, timestamps from filesystem

## Done (continued)

- **"There are no skills — only agents" reframe** — collapsed the two-tier model (skills + agents) into one primitive: the agent. Skills are narrow-scope agents. Captured in SEMANTICS-NOTES §13.
- **FP correspondence** — mapped agent composition to FP: monads, do-notation, regions, closures, typeclasses. SEMANTICS-NOTES §13.1–§13.3.
- **Composition operators** — named the operator set (`>>`, `>>=`, `|||`, `case`, `fix`, `wait`, `try`). Expressed CDD as one composition expression. SEMANTICS-NOTES §14.
- **Prior art survey** — Erlang/OTP (supervision trees, behaviours), process calculi (session types, choreography), Rx (observable streams, cancellation), Schlapbach 2026 (π-calculus for agentic protocols). SEMANTICS-NOTES §14.4.
- **SOUL as agent type** — SOUL is not a module, it's a type constraint. Typeclass definition (Haskell), behaviour spec (Erlang), typing context Γ. Agent = type instance + composition body. SEMANTICS-NOTES §14.5–§14.6.
- **LANGUAGE-SPEC v0.2 draft updated** — two kinds (agent-type, agent-module), `type` in required signature fields, composition operators in §6 with examples.
- **Issue #284 rewritten** — from "add frontmatter to SOUL" to "express SOUL as type, not module"
- **Issue #285 expanded** — now covers cnos.cdd alongside cnos.core, includes composition operators
- **6 adhoc threads** committed to hub

## Tomorrow

- Monitor γ progress on #283 and #284
- Check if #277 and #284 should merge
- SEMANTICS-NOTES and LANGUAGE-SPEC v0.2 draft are in cnos working tree — need CDD cycle or operator commit
