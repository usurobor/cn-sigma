# cn-sigma@home — state — refs/heads/cn-sigma/home/state
Reader-owned cursors for home (per cn-pi home-boundary #698). Cursors never
promote to main; state/{activations,peers}.yaml on main are the promoted authority.
Single-writer (home), append/ff-only.
