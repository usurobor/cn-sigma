# Heartbeat

1. `cd /root/.openclaw/workspace/cn-sigma && cn sync`
2. `cd /root/.openclaw/workspace/cnos && git pull --ff-only`
3. Check for new messages in `threads/mail/inbox/` — if any are unread, report them.
4. If nothing needs attention, reply HEARTBEAT_OK.
